# 产品说明：复航台

## 一句话定义

复航台为频繁切换项目的知识工作者保存“离开时的认知现场”，并把重新进入状态压缩成一张 60 秒复航卡。

## 核心问题

普通待办工具能回答“还要做什么”，但很少能回答：

- 上次为什么停在这里？
- 哪些判断已经做过，不应该重新推导？
- 哪个问题仍未解决？
- 我现在打开文件后，第一件具体的事是什么？

这种隐形的上下文重建成本，使短时间工作窗口变得不值得开始，也让多项目切换产生持续疲劳。

## 设计原则

1. **记录必须比遗忘便宜。** 高频记录入口应在两次操作内完成。
2. **下一步必须是物理动作。** “继续研究”不是好提示，“比较 A/B 两份结果并写下差异”才是。
3. **复航卡解释来源。** 程序整理已有记录，不假装理解用户没有记录的上下文。
4. **本地优先。** 未成形的想法和工作轨迹默认不离开设备。
5. **温和提醒，不制造焦虑。** “久未复航”是信息，不是红色惩罚。
6. **键盘与移动端都能完成闭环。** 桌面端追求速度，移动端适合离场时补记。
7. **新证据优先，但不改写历史。** 每条复航结论都能回溯到带时间的项目、轨迹或检查点证据。
8. **损坏数据应显式失败。** 不用静默归一化换取“看起来能打开”；原文和上一份有效快照应优先保留。

## 主要对象

- **项目 Project**：需要多次工作会话才能推进的一项工作。
- **会话 Session**：一次有明确意图的连续工作时段。
- **面包屑 Crumb**：工作过程中留下的短记录，类型包括笔记、发现、决定、问题、阻塞和下一步。
- **检查点 Checkpoint**：离开项目时保存的结构化状态快照。
- **复航卡 Reentry Card**：由最新检查点和近期重要面包屑组成的只读恢复视图。

## 首个版本的完成定义

- 能创建、切换和归档项目。
- 能开始会话、计时、快速留下不同类型的面包屑。
- 能通过检查点结束会话，并明确下一物理动作。
- 再次进入项目时能立即看到准确的复航卡。
- 刷新和关闭页面不会丢失活动会话或计时信息。
- 能导出、校验并恢复完整备份；正文生成时间与本地日期文件名绑定同一个单调有效时刻，系统时钟回拨也不漂移。
- 核心整理逻辑具有自动化测试。
- 桌面和窄屏均可使用，基础键盘操作与辅助技术可访问；全局命令只使用带修饰键的组合，不劫持语音或辅助输入产生的裸字符；工作流模态框会同时读出标题和可见指导语，连续搜索、数据保护与操作反馈各自只通过单一通道播报最新的简短摘要，延迟的数据保护结果不重建后来打开的界面，旧界面的延迟命令也不会跨重绘执行。
- 安装式离线外壳按版本原子更新；在线内容保持网络优先，但不能把已安装缓存逐文件改写成混合版本，异常缓存打开或读取也不能让回退请求永久悬挂。

以上首版闭环已经完成。当前迭代重点是：真实中断时不丢现场、复航卡中的每条信息都有可靠时间来源，以及项目规模增长后仍能快速找回上下文。

## 后续迭代池

- 可信复航卡（已完成）：检查点后的变化、来源时间、已解决问题
- 全局搜索、键盘入口、跨项目快捷捕获与常用动作命令面板（已完成）；快捷捕获在多标签重绘后保留筛选与有效目标，失效目标必须明确重选
- 项目休眠、阻塞时长与“失联项目”视图（已由关注清单覆盖基础场景，仍可深化）
- 七日会话统计、近距切换与周内回顾（已完成基础版）
- 链接识别、置顶记录、安全起步模板，以及携带项目状态和现场更新时间的活动会话、普通/归档项目与工作区 Markdown 交接文档（已完成）；持久模板库待迭代
- 滚动快照与撤销/重做、导入冲突预览、备份内容校验码（已完成）；回收站和多代快照待迭代
- 可选的加密同步或本地桌面封装
- 大工作区性能（时间线/项目集合窗口、批量复航索引与状态级搜索索引已完成）
