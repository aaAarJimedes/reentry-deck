# 复航台 · REENTRY 交接草稿

生成日期：2026-08-28（Asia/Shanghai）  
范围冻结版本：0.193.0  
当前分支：`main`

## 项目介绍

复航台是一个本地优先、零运行时依赖的工作上下文恢复器。它把长期工作的项目、专注会话、过程轨迹、可靠检查点与“回来后的第一物理动作”保存在当前浏览器，通过复航卡、应急停靠、跨项目快捷记录、搜索、归档和本地 JSON 备份，帮助用户在中断后恢复真实工作现场。

程序由原生 HTML、CSS 和 ES modules 构成；本地开发服务器、测试与发布契约只使用 Node.js 内建能力。没有账号、云数据库、外部 API、`node_modules` 或编译产物。

## 本次持续目标与真实时段

本项目就是“创造一个实用程序并持续完善至少 10 小时，直到用户指示停止”的目标成果，不是旁支任务。

- 目标建立时间：2026-08-28 00:22:00 +08:00。
- 目标计时器在收尾盘点时记录的有效工作时长：37,964 秒，即 10 小时 32 分 44 秒；已满足至少 10 小时要求。
- 仓库首个实现提交：2026-08-28 00:43:49 +08:00，`3d106a6 feat: build initial Reentry Deck workflow`。
- 范围冻结前最终功能提交：2026-08-28 14:26:10 +08:00，`858b148c9f1fceddcac2dc6d2ecd16346baa0b40`。
- 墙钟区间包含宿主卸载、暂停和恢复，不把这些中断时间冒充为有效工作时长。

## 整个目标时段完成的工作

### 产品与核心闭环

- 从零构想并实现“复航台”：项目 → 会话 → 轨迹 → 检查点 → 60 秒复航卡的完整闭环。
- 实现项目状态、可恢复归档、会话计时、发现/决定/问题/阻塞/下一步轨迹、置顶航标、问题解决与重开。
- 实现人工可靠检查点、低置信度快速停靠、跨天/超时会话接续、快速检查点复核升级和证据化复航完整度。
- 实现跨项目快捷记录、全局搜索/命令面板、工作区交接清单、项目复航简报、七日航迹和关注清单。

### 数据安全与恢复

- 建立严格、共享的模型/导入校验：普通 JSON 对象、字段白名单、枚举、引用、唯一 ID、50,000 条总记录上限、字段容量和因果时间窗口。
- 使用单调修订号、逻辑时间锚点、跨标签短写入租约、提交前后复核，阻止陈旧标签覆盖新数据。
- 实现主数据损坏时的滚动快照恢复、可逆撤销/重做、浏览器配额临界回收，以及失败时不发布未持久化内存状态。
- 实现 25 MB 备份读取上限、流式 UTF-8 解码、FNV-1a 内容校验、导入差异预览、最后请求获胜和销毁时异步取消。

### 出口、输入与浏览器可靠性

- 严格处理畸形哈希路由、百分号 URL、凭据、控制字符、混合标签主机、超长资源和安全展示标签。
- 剪贴板、下载文件名、交接文本和诊断提示均采用有界源扫描，保护完整 Unicode 并拒绝或修复孤立代理项。
- 下载名避让路径禁用字符、Windows 经典/控制台/上标数字设备别名；对象 URL 在成功与异常路径均可靠回收。
- 错误投影只读取一次异常 `message`，吸收抛错 getter，将 1,000 单位原始窗压到 500 单位可见说明。

### 性能、界面与发布

- 对 50,000 条边界工作区采用流式选择、有界堆/窗口、分页 DOM、共享索引与跨投影单次扫描，避免无界中间数组和重复全量过滤。
- 完成桌面/移动响应式界面、明暗/跟随系统主题、减少动效、可访问名称、焦点恢复、模态边界与状态播报。
- 修复深色主题复航主卡低对比，真实浏览器验证固定深森林局部表面。
- 实现零依赖白名单 HTTP 服务、CSP/权限/防嵌入响应头、PWA manifest 与原子离线外壳；发布契约自动核对版本、模块可达性、HTTP 白名单和缓存清单。
- 为应用销毁和首次构造建立资源回滚：监听器、媒体查询、状态订阅、计时器、异步请求和提示任务均可释放。

完整逐版本细节见 `CHANGELOG.md`，架构边界见 `docs/ARCHITECTURE.md`，手工验收步骤见 `docs/QA.md`。

## 截至目前正在做 / 刚完成什么

范围已冻结，刚完成 0.193.0“原子界面启动”：界面初始化任一步失败时调用幂等销毁路径回收已安装资源，再把原始异常交给静态恢复界面；清理异常不遮蔽真正启动原因。

收尾前紧邻完成：

- 0.191.0：完整避让 Windows 设备文件名别名。
- 0.192.0：统一、有界且不可再抛错的诊断投影。
- 0.193.0：首次界面构造失败自动回滚资源。

不再开启新功能或下一轮持续迭代。

## 剩余 TODO（全部冻结或等待授权）

1. 等待用户指定/批准 GitHub 远端；当前没有 `origin` 或任何其他 remote，不能执行 push。
2. 等待用户批准后，才按下方拟议清单移动或复制资产；本次没有执行任何移动、复制或删除。
3. 可选人工验收：在用户选定的交付浏览器中执行安装式 PWA、完全离线重启和站点数据恢复演练。自动服务工作线程测试已经覆盖对应故障路径，但本次不扩大收尾范围。
4. 当前无已知失败测试、未解决实现阻塞或必须补做的构建步骤。

## 测试、构建与运行结果

- `npm test`：360 tests / 28 suites，360 通过，0 失败、0 跳过、0 取消。
- `npm run check`：全部应用、核心模块、服务器与服务工作线程通过 `node --check`。
- 发布契约：版本号、备份 `APP_VERSION`、最新 CHANGELOG、运行时模块依赖、HTTP 白名单与离线外壳一致。
- 真实浏览器：`http://localhost:4173/#/settings` 重载后 `appReady=true`、`startupFailure=false`、深色主题保持、无横向溢出。
- 构建：项目无需编译/打包；可交付运行形态就是受版本控制的静态文件。没有生成重复构建包。

## Git 状态与待推送状态

- 分支：`main`。
- 上游：未配置。
- GitHub 远端 URL：未配置；`git remote -v` 无输出，`git remote get-url origin` 返回 `NO_ORIGIN`。
- 范围冻结前最终功能提交：`858b148c9f1fceddcac2dc6d2ecd16346baa0b40`。
- 本交接草稿将单独作为本地收尾提交；由于提交哈希包含本文件内容，文件自身无法预先嵌入其最终提交哈希而不形成自引用。提交完成后的真实最终 `HEAD` 会在任务最后回复中明确给出，并可用 `git rev-parse HEAD` 本地复核。
- 待推送状态：全部成果仅在本地；没有执行 push，也没有可用远端目标。

## 资产盘点与拟议路径（仅计划，未执行迁移）

盘点时仓库共有 52 个非 `.git` 文件、约 889,505 bytes；`.git` 约 1,130,741 bytes；合计约 2,020,246 bytes（约 1.93 MiB）。新增本草稿后会略增，但仍远低于 500 MB 监视阈值。项目中没有 `node_modules`、coverage 历史、日志、截图集、录屏或构建包。

### `D:\Agent Workspaces\Agent Tools\reentry-deck`

拟放入可编辑的完整 Git 仓库，不拆散历史：

- `.git/`：本地提交历史、分支和仓库元数据。
- `src/`、`index.html`、`sw.js`、`app.webmanifest`、`assets/icon.svg`：应用源码与静态运行资产。
- `tests/`：Node 原生测试套件。
- `tools/server.mjs`：零依赖本地 HTTP 服务。
- `package.json`、`.gitignore`：运行/检查配置。
- `README.md`、`PRODUCT.md`、`CHANGELOG.md`、`docs/`：产品、架构、变更和 QA 文档。

建议迁移单位：整个当前仓库目录，而不是逐文件移动；等待用户批准后再执行。

### `D:\Agent Workspaces\Agent Delivery\reentry-deck`

拟放入交付与交接材料：

- `HANDOFF_DRAFT.md`：本报告草稿。
- 可选的未来 `reentry-deck-0.193.0.zip`：仅在用户明确要求发布包后，从已提交静态文件生成一次；当前不存在，也未生成。
- 可选的校验清单：仅在实际生成发布包时一并产生；当前不存在。

### `D:\Agent Workspaces\Agent Temp\reentry-deck`

当前没有需要迁移的临时资产。该路径只拟用于未来可再生成的测试缓存或媒体临时产物：

- 允许：一次性浏览器录制、可再生大截图集、覆盖率临时输出、测试缓存。
- 清理条件：对应验证完成、结论已写入交付报告且无需复现后，经用户授权再清理。
- 当前项目中没有上述文件，因此本次不移动、不复制、不删除任何内容。

## 安全收尾声明

- 未执行外部 push、远端变更或强推。
- 未移动、复制或删除源码、用户数据、未提交成果或缓存。
- 未创建或调用任何子代理。
- 范围已冻结；完成本报告提交和最终状态复核后停止运行，等待用户明确批准 push 与资产迁移。
