# 复航台 · REENTRY 交接草稿

生成日期：2026-08-28（Asia/Shanghai）；维护更新：2026-08-31

历史交接基线：0.193.0；当前维护版本：0.250.0

当前开发分支：`feature/0.250-rollback-integrity`（从本地 0.249.0 提交 `052c8c26320bbc7dfa1b81b4f2173016a59d9327` 分出；完成后等待按版本顺序发布）

> 状态更新：持续维护以仓库、测试和 GitHub 实时状态为唯一交接依据。0.244.0 已在本地完成提交与注释标签，0.245.0、0.246.0、0.247.0、0.248.0 与 0.249.0 已在后续独立提交完成；远端仍停在 0.243.0，且此前没有执行部分替代写入。`origin` 现已配置仓库级持久 SSH-over-443，本轮完成 0.250.0 最终提交后将直接按版本顺序同步，无需再次运行 GitHub CLI 登录；同步完成即停止维护并等待后续指示。

## 项目介绍

复航台是一个本地优先、零运行时依赖的工作上下文恢复器。它把长期工作的项目、专注会话、过程轨迹、可靠检查点与“回来后的第一物理动作”保存在当前浏览器，通过复航卡、应急停靠、跨项目快捷记录、搜索、归档和本地 JSON 备份，帮助用户在中断后恢复真实工作现场。

程序由原生 HTML、CSS 和 ES modules 构成；本地开发服务器、测试与发布契约只使用 Node.js 内建能力。没有账号、云数据库、外部 API、`node_modules` 或编译产物。

## 本次持续目标与真实时段

本项目就是“创造一个实用程序并持续完善至少 10 小时，直到用户指示停止”的目标成果，不是旁支任务。

- 目标建立时间：2026-08-28 00:22:00 +08:00。
- 目标计时器在收尾盘点时记录的有效工作时长：37,964 秒，即 10 小时 32 分 44 秒；已满足至少 10 小时要求。
- 仓库首个实现提交：2026-08-28 00:43:49 +08:00，`3d106a6 feat: build initial Reentry Deck workflow`。
- 范围冻结前最终功能提交：2026-08-28 14:26:10 +08:00，`858b148c9f1fceddcac2dc6d2ecd16346baa0b40`。
- 墙钟区间包含宿主卸载、暂停和恢复，不把这些中断时间冒充为有效工作时长。

## 整个目标时段完成的工作

### 产品与核心闭环

- 从零构想并实现“复航台”：项目 → 会话 → 轨迹 → 检查点 → 60 秒复航卡的完整闭环。
- 实现项目状态、可恢复归档、会话计时、发现/决定/问题/阻塞/下一步轨迹、置顶航标、问题解决与重开。
- 实现人工可靠检查点、低置信度快速停靠、跨天/超时会话接续、快速检查点复核升级和证据化复航完整度。
- 实现跨项目快捷记录、全局搜索/命令面板、工作区交接清单、项目复航简报、七日航迹和关注清单。

### 数据安全与恢复

- 建立严格、共享的模型/导入校验：普通 JSON 对象、字段白名单、枚举、引用、唯一 ID、50,000 条总记录上限、字段容量和因果时间窗口。
- 使用单调修订号、逻辑时间锚点、跨标签短写入租约、提交前后复核，阻止陈旧标签覆盖新数据。
- 实现主数据损坏时的滚动快照恢复、可逆撤销/重做、浏览器配额临界回收，以及失败时不发布未持久化内存状态。
- 实现 25 MB 备份读取上限、流式 UTF-8 解码、FNV-1a 内容校验、导入差异预览、最后请求获胜和销毁时异步取消。

### 出口、输入与浏览器可靠性

- 严格处理畸形哈希路由、百分号 URL、凭据、控制字符、混合标签主机、超长资源和安全展示标签。
- 剪贴板、下载文件名、交接文本和诊断提示均采用有界源扫描，保护完整 Unicode 并拒绝或修复孤立代理项。
- 下载名避让路径禁用字符、Windows 经典/控制台/上标数字设备别名；对象 URL 在成功与异常路径均可靠回收。
- 错误投影只读取一次异常 `message`，吸收抛错 getter，将 1,000 单位原始窗压到 500 单位可见说明。

### 性能、界面与发布

- 对 50,000 条边界工作区采用流式选择、有界堆/窗口、分页 DOM、共享索引与跨投影单次扫描，避免无界中间数组和重复全量过滤。
- 完成桌面/移动响应式界面、明暗/跟随系统主题、减少动效、可访问名称、焦点恢复、模态边界与状态播报。
- 修复深色主题复航主卡低对比，真实浏览器验证固定深森林局部表面。
- 实现零依赖白名单 HTTP 服务、CSP/权限/防嵌入响应头、PWA manifest 与原子离线外壳；发布契约自动核对版本、模块可达性、HTTP 白名单和缓存清单。
- 为应用销毁和首次构造建立资源回滚：监听器、媒体查询、状态订阅、计时器、异步请求和提示任务均可释放。

完整逐版本细节见 `CHANGELOG.md`，架构边界见 `docs/ARCHITECTURE.md`，手工验收步骤见 `docs/QA.md`。

## 截至目前正在做 / 刚完成什么

当前维护轮次正在收口 0.250.0“滚动快照写失败不再冒充上一次保存”：主工作区元数据继续保持 0.249.0 的严格字段形状，滚动快照来源改由独立格式标记和最多两项的校验账本记录，避免已缓存旧页面因未知字段拒绝并覆盖新版数据。账本分别保留当前与待提交主原文的前版关系，格式与账本都不存在才进入旧兼容；现代缺件、畸形与一般不匹配副本失败关闭。旧页面写入若恰好打断一个在途新版保存，只有物理前版、残留待提交账本、直接后继修订号和不倒退时间共同唯一证明关系时，才在恢复租约与完整事实复核后狭窄重建；其他情况等新版再次保存。滚动副本覆盖、清理或主配额重试时，主数据、副本、账本和写租约会重复核对；合法外部胜者只按自己的可验证账本或上述唯一可归因事实保留共同前版，不继承失败标签页的其他历史。待提交主原文在取租约前完成序列化，来源账本与校验在对应关键阶段的最后续期和事实复核前计算；清理或来源恢复跨入 500 ms 安全余量/5 秒期限后，只接受精确且仍安全有效的同所有者令牌，或用刷新后的时钟重新取得恢复租约并复核全部事实。续期/恢复令牌无论正常返回还是写入后抛错，只要调用本身耗尽租期就拒绝采用；恢复令牌可再做一次有界的新时钟尝试。最终释放不删除已过期、临近过期或被竞争者替换的令牌。损坏替换只有在账本仍认可最后确认原文且副本槽为空时才尽力补回。空字符串、畸形 JSON 与可解析结构损坏统一按独立账本恢复；损坏启动会记住确切已接受原文，使首次修复保存成功后仍可切回，修复中止、删除生效后抛错或再次损坏替换也不丢证明。孤立副本不会进入全新工作区，撤销与导入提示只有提交后再次确认快照可用时才承诺可切回。Web Storage 不提供跨键 compare-and-swap；单次调用跨完整租期、最终事实读到变更调用之间的直接改写或连续恢复调用都耗尽租期时只能失败关闭，不能宣称事务原子性。

本轮维护前紧邻完成的 0.249.0 已提交为 `052c8c26320bbc7dfa1b81b4f2173016a59d9327`：同会话内联轨迹草稿跨无关本地重绘、模态覆盖和外部更新保持，迟到导入读取也不会替换后来打开或正等待恢复的模态；真实浏览器定向证据见下。0.248.0 的只读 CI 仍尚无远端 Actions 运行证据，不能把本地通过描述成 GitHub 绿色。

0.227.0 候选审计曾把公开 GitHub 提交 `c3f9d5efdd496ec43c51f2b5fcc81ad09447e426` 的对话框与键盘可达性范围交给固定 `gemini-3.7-flash-high` 做一次只读评审；调用已提交但结构化输出为无效 JSON，桥接器判为 `contract_invalid`，没有业务输出或采用结论，也未自动重试。对应目标由父任务独立复核、实现和验收。

本轮维护前紧邻完成：

- 0.191.0：完整避让 Windows 设备文件名别名。
- 0.192.0：统一、有界且不可再抛错的诊断投影。
- 0.193.0：首次界面构造失败自动回滚资源。
- 0.194.0：备份导入、示例和撤销恢复路径同享持久存储请求。
- 0.195.0：数据保险箱提供可访问的持久保护状态与主动请求入口。
- 0.196.0：启动时只读恢复已有浏览器保护状态。
- 0.197.0：部分浏览器缺失检查接口时仍保留主动保护请求。
- 0.198.0：延迟保护状态检查重绘不再打断暂态输入。
- 0.199.0：标签页恢复可见时补偿核对实际持久化工作区。
- 0.200.0：后退/前进缓存恢复时补偿核对工作区与计时器。
- 0.201.0：离线缓存世代绑定语义版本，并固定 JavaScript/ESM 的 LF 检出。
- 0.202.0：把 PWA 启动范围、文档元数据与安装图标纳入发布契约。
- 0.203.0：移除运行时内联样式能力并收紧为同源样式 CSP。
- 0.204.0：活动与归档项目可安全复用为不含历史的新现场模板，并修复 Escape 暂态清理。
- 0.205.0：`PORT=0` 报告操作系统实际绑定端口，并规范投影 IPv4 / IPv6 地址。
- 0.206.0：端口冲突和权限不足显示可操作的一行提示，未知启动故障保留完整堆栈。
- 0.207.0：`HOST` 在 DNS 前完成有界主机/IP 规范化，空值与 URL 形态原子拒绝。
- 0.208.0：复航卡分离三条正文窗口与完整未决汇总，页面、推荐和关注清单使用真实总量。
- 0.209.0：复制复航简报保留精确未决余量，并在 800 字符内公平压缩三条正文。
- 0.210.0：关注评分同时返回四项最优窗口和完整风险项目总数，舰桥与交接清单披露余量。
- 0.211.0：全局搜索同时返回四十项最优窗口和完整命中总数，搜索面板区分总数与显示数。
- 0.212.0：复航卡的三条置顶正文窗口同时保留完整航标总数，并披露窗口外余量。
- 0.213.0：复航卡的三条检查点后变化窗口同时保留完整变化总数，并披露窗口外余量。
- 0.214.0：复航卡的两条最近决定窗口同时保留完整决定总数，详情统计复用同一标量。
- 0.215.0：检查点后的未收拢/中断会话只保留三项兼容证据对象，同时为完整度、缺口和警告保留精确总数。
- 0.216.0：项目复航卡可下载转义安全的 Markdown 简报，正文与复制简报共用有界证据投影和精确未决余量。
- 0.217.0：关注清单三项原因窗口增加精确总数，舰桥和工作区交接都披露窗口外现场缺口。
- 0.218.0：工作区交接清单可下载为转义安全的 Markdown，继续复用五项排名、四项关注、三条原因与完整余量边界。
- 0.219.0：三类导出文件名统一使用严格本地日历日期，在 UTC 跨日窗口仍与用户看到的日期一致。
- 0.220.0：归档项目详情直接提供带上下文可访问名称的复制与 Markdown 下载，无需恢复或改变项目状态。
- 0.221.0：活动会话专注面板保留同一套复制与 Markdown 下载入口，导出不打断计时或会话状态。
- 0.222.0：完整 JSON 备份只采样一次导出时刻，信封 `exportedAt` 与本地日期文件名保持同源。
- 0.223.0：备份信封与文件名共享存储层单调有效时刻，系统时钟回拨也保持日期一致。
- 0.224.0：项目纯文本与 Markdown 交接正文携带规范生命周期状态，归档语义不再只存在于界面按钮。
- 0.225.0：两种项目交接正文携带严格毫秒 UTC 现场更新时间，畸形值安全降级且不执行强制转换。
- 0.226.0：五个工作流对话框复用可见指导语作为无障碍描述，并由逐入口契约防止关联回退。
- 0.227.0：搜索结果列表退出实时区域，独立原子状态只播报同一次结果投影生成的简短摘要。
- 0.228.0：可见 toast 与通用辅助反馈统一为单一实时通道，专用短语不再被视觉说明重复播报。
- 0.229.0：通用实时播报采用最新请求获胜的帧提交，连续操作、重绘或销毁后旧消息不再迟到写入。
- 0.230.0：命令面板的下一帧动作绑定发起时的渲染世代，旧界面命令不再跨重绘执行。
- 0.231.0：本机数据保护的可见说明退出局部实时区域，异步检查和手动请求统一通过通用单通道播报。
- 0.232.0：在线新响应不再逐文件改写已安装版本缓存，断网回退始终保持一套完整离线外壳。
- 0.233.0：快捷记录在外部重绘后重建筛选窗口并保留有效目标，失效目标持续要求明确重选。
- 0.234.0：裸字符退出全局命令层，保留修饰键组合；快速检查点复核可靠聚焦可编程复航卡标题。
- 0.235.0：持久保护检查与主动请求原地同步有限视图，不再重建等待期间形成的交互上下文。
- 0.236.0：运行期离线壳单项读取拥有独立 750 ms 活性上界，异常缓存后端不再让导航或模块永久悬挂。
- 0.237.0：持久保护异步反馈复用解析后的设置页语义，规范与短哈希都能结束检查；原地视图测试禁止根节点重建并覆盖缺件零写入。
- 0.238.0：活动轨迹输入焦点与实际本地提交绑定；陈旧标签的外部采用后拒绝、直接失败、静默返回和销毁都不会把目标泄漏给新会话或后续重绘。
- 0.239.0：应急停靠的正文与接续输入目标也绑定实际本地提交；失败旧操作不能再把焦点投进外部工作区或较新的活动会话。
- 0.240.0：人工可靠检查点的正文焦点绑定实际本地提交；冲突或直接失败保留/失效既有对话框上下文，不向外部页面或后续重绘泄漏焦点。
- 0.241.0：跨项目快捷记录的顶栏入口焦点绑定实际本地提交；冲突或直接失败保留有界表单上下文，不从失败表单跳回顶栏或在后续重绘迟到聚焦。
- 0.242.0：修订号后继统一受安全整数上限保护；合法最大值仍可读取与导出，所有后继写入原子失败，外部清空保留可导出内存现场；撤销和导入焦点及预览刷新绑定实际提交结果。
- 0.243.0：开始会话的焦点按项目页本地提交与跨路由导航分流；首页请求同时绑定目标哈希、新会话身份和精确表单，失败、错配、绕行或销毁均不会把旧焦点投进外部现场。
- 0.244.0：五类项目持久化动作的按钮、动态控件或正文焦点绑定实际本地提交；外部采用后拒绝、直接失败和后续无关重绘不再领取旧目标。
- 0.245.0：主题、动态效果与离开提醒阈值的原控件焦点绑定实际本地提交；冲突采用、直接失败、静默返回、销毁与后续无关重绘不再领取失败目标。
- 0.246.0：数据保险箱提供真实事件驱动的一次性 PWA 安装入口；接受、取消和安装完成严格分离，迟到结果受代际与销毁门保护，安装状态不写入工作区且只原地更新稳定设置行。
- 0.247.0：会话状态、默认活动语义与关闭原因共享严格生命周期契约；非法组合在持久化前原子拒绝，旧备份缺失或为 `null` 的原因继续无推断兼容。
- 0.248.0：`main` 推送与拉取请求进入固定 Ubuntu 24.04 / Node 22 的只读 CI，完整不可变工作流结构由零依赖本地契约锁定。
- 0.249.0：活动会话内联轨迹正文与类型跨无关本地重绘、模态覆盖和外部更新保持；外部更新还恢复仍聚焦输入的选区，成功记录或现场替换才清除草稿，迟到导入预览不替换后来打开或正等待恢复的模态。
- 0.250.0：向旧缓存客户端不可见的双项侧边账本绑定可信滚动前版或声明无快照；失败覆盖、外部替换、损坏启动与配额重试只在来源和身份允许时恢复、回收或暴露副本，撤销与导入提示改为提交后条件确认。

本版完成最终验证与本地提交后，使用现有 SSH `origin` 先完整发布本地 0.244.0，再按顺序快进、标记并发布 0.245.0、0.246.0、0.247.0、0.248.0、0.249.0 与 0.250.0。0.248.0 起的每个提交都先推到 `main` 并等待对应 CI 绿色，再推送注释标签和创建 Release。本轮同步完成后不再开启下一维护版本，等待用户后续指示。

## 剩余 TODO

1. 发布执行：最终提交后使用现有 SSH-over-443 `origin`，严格按 0.244.0 → 0.245.0 → 0.246.0 → 0.247.0 → 0.248.0 → 0.249.0 → 0.250.0 顺序完成快进、注释标签与 Release；0.248.0、0.249.0 与 0.250.0 各自先等待对应 `main` CI 绿色再标记和发布，不执行乱序或部分替代写入。
2. 后续独立 UI 候选：保留导入读取提示跨后续外部重绘、合并导入预览失败/重开的双动画帧焦点请求、让搜索对话框恢复查询时同步恢复结果列表，以及在快速检查点复核上下文失效时明确提示而非静默丢弃草稿；每项应独立失败优先，不混入 0.250.0。
3. 可选人工验收仍保留：在用户选定的交付浏览器中实际确认安装提示、完成安装、完全离线重启和站点数据恢复演练；0.246.0 的真实浏览器验收保持只读，没有合成事件或点击安装，自动状态机与服务工作线程测试覆盖对应边界。
4. 资产迁移仍未获授权，本轮不移动或复制下方盘点内容；只清理确认不再使用且可再生的残留。

## 测试、构建与运行结果

- `npm test`：540 tests / 32 suites，540 通过，0 失败、0 跳过、0 取消。
- `npm run check`：全部应用、核心模块、服务器与服务工作线程通过 `node --check`。
- 发布契约：版本号、备份 `APP_VERSION`、最新 CHANGELOG、运行时模块依赖、HTTP 白名单与离线外壳一致。
- 会话闭合定向回归：新增 9 项契约，模型、存储与界面相关组合 200/200 通过；另有发布契约 5/5。覆盖完整状态/原因矩阵、缺失状态的全局活动默认、falsy 非空原因、工厂清理、旧数据归一化与继续写入、更新/替换零存储调用原子拒绝，以及人工检查点、普通/接续停靠和示例生产者。
- 内联草稿定向回归：界面与焦点门组合 93/93 通过，其中两项真实 `AppStore` 测试覆盖无关本地重绘、成功记录消费、陈旧标签冲突中的同步外部采用及异常后复位；恢复门测试覆盖连续保留型重绘的快照/提示传递、请求身份消费、显式清理与销毁语义。源码契约覆盖模态背景、空正文类型、同会话身份、Unicode 限窗、类型白名单、焦点/选区、继续会话、时间线展开、三条导入直接重绘，以及文件读取期间已打开或正等待恢复模态的非替换边界。
- 滚动快照来源定向回归：模型、导入、存储与界面契约组合 274/274（12 suites）通过，其中存储单套 141/141。除旧缓存字段兼容、双项账本、损坏启动与条件式提示外，还覆盖主数据/副本/来源/清理/配额恢复在 4.6/6 秒、正常返回、写入后抛错和合作标签接管下的精确事实修复；独立终审横扫 52 个存储操作边界，未发现可实现的合作式 P1/P2。
- CI 工作流契约：1/1 通过。契约锁定唯一且不超过 16 KiB 的 ASCII YAML、两个 `main` 触发器、唯一只读权限、同引用并发取消、固定 Ubuntu 24.04 / Node 22、十分钟上限、两项 action 的完整提交、凭据与缓存关闭和五个有序步骤；setup-node 后的直接契约预检必须先于 npm 脚本，并同时拒绝依赖字段、根目录 `.npmrc`、锁文件、`node_modules`、npm 生命周期钩子、不安全检查段与任一源码静态检查目标遗漏。当前尚无远端 Actions 运行结果。
- 真实浏览器（最近完整视口基线）：0.246.0 已在全新随机来源 `http://127.0.0.1:9886/` 完成只读发行验收。首次检查发现按钮虽有 `hidden` 属性却被共享 `inline-flex` 样式显示，随即增加全局 `[hidden]` 规则与自动回归；复验后 1280×720、800×720 与 390×844 的数据保险箱均保持单一主区域、无打开模态和零横向溢出。浏览器未自然交付安装提示时，如实说明浏览器菜单入口，按钮实际计算为 `display:none`、`tabIndex=-1`；没有合成事件、点击安装、提交设置、创建项目、下载或工作区写入。浏览器实际提供的 `store.js` / `sw.js` 确认 `APP_VERSION` / `BUILD_ID` 0.246.0，离线壳包含新模块，控制器保留 accepted/installed 区分并在首次等待前调用 prompt；`app.js` 与 CSS 确认两类浏览器监听、保守说明和全局隐藏规则。应用页及五个源码页日志均为空。临时视口已复位，全部七个验收/空白标签页已关闭，端口 9886 确认无监听。
- 真实浏览器（0.247.0 定向）：在全新来源 `http://127.0.0.1:9887/` 通过真实“载入示例现场”动作生成 2 个项目与 1 个可靠检查点，整页重载后项目、检查点和复航内容仍在，载入前后页面日志均为空；没有读取浏览器内部存储、合成事件、下载或触碰既有来源。临时标签已关闭，端口 9887 确认无监听。
- 0.248.0 不改变界面、数据模型或浏览器 API，未重复视口验收；`sw.js` 只按正式版本契约进入新的离线缓存世代。
- 真实浏览器（0.249.0 定向）：在全新来源 `http://127.0.0.1:9888/` 载入示例并启动会话后，带正文和 `decision` 类型的草稿跨置顶、问题解决、项目状态更新、模态快捷记录保持；空正文但 `question` 类型的草稿也跨本地动作保持。成功内联记录后正文清空、类型复位且新记录只出现一次。第二标签更新时，第一标签的 `blocker` 类型、完整正文、0–10 选区和输入焦点全部恢复，两页日志为空。未读取浏览器内部存储、未合成事件、未下载；全部标签已关闭，端口 9888 确认无监听。
- 真实浏览器（0.250.0 定向）：在本地来源 `http://127.0.0.1:9889/` 打开数据保险箱，页面显示条件式“若新快照成功建立，可再次切换”说明；无快照时可见“回到上次保存”动作保持禁用，桌面视口零横向溢出。浏览器实际提供的 `store.js` / `sw.js` 确认 `APP_VERSION` / `BUILD_ID` 均为 0.250.0，页面日志为空。此项只验证真实 DOM、可见状态与服务内容；存储竞态来自自动化故障注入，不冒充浏览器时序模拟。验收标签已关闭，端口 9889 确认无监听。
- 构建：项目无需编译/打包；可交付运行形态就是受版本控制的静态文件。没有生成重复构建包。

## Git 状态与同步状态

- 当前开发分支：`feature/0.250-rollback-integrity`，以本地 0.249.0 提交 `052c8c26320bbc7dfa1b81b4f2173016a59d9327` 为基线；0.250.0 验证后先保留为独立提交，不越过尚未同步的 0.244.0–0.249.0 发布顺序。
- 上游：`origin/main`。
- GitHub 远端 URL：`ssh://git@ssh.github.com:443/aaAarJimedes/reentry-deck.git`。
- 0.244.0 开发前基线：`f740f906befff6fe82340cf0a78e61dbee27e9eb`。
- 本地 `main` 与注释标签 `v0.244.0` 均指向 `ffabc095f70e4fff72cbda2a11ca284b15645e24`；`origin/main` 在本轮同步前仍为上述 0.243.0 基线，远端尚无 0.244.0–0.250.0。此前 HTTPS/GitHub CLI 路径在任何远端写入前失败，因此没有部分替代写入；当前 `origin` 已切到仓库级持久 SSH-over-443，最终发布直接使用该配置。
- 本交接草稿随版本提交持续维护；由于提交哈希包含本文件内容，文件自身无法预先嵌入其最终提交哈希而不形成自引用。真实发布 `HEAD` 可用 `git rev-parse HEAD` 本地复核。
- 同步目标：先以非强制快进方式推送现有 `main` 与注释标签 `v0.244.0` 并建立同名 Release；再依次快进本地 `main` 到 0.245.0、0.246.0 与 0.247.0 验证提交，各自创建并推送同名注释标签及 Release。随后依次快进到 0.248.0、0.249.0 与 0.250.0，每版都等待对应 `main` CI 绿色后再推送注释标签并创建 Release。仓库无需编译产物，Release 使用 GitHub 自动生成的源代码归档，不额外复制手工 ZIP。

## 资产盘点与拟议路径（仅计划，未执行迁移）

0.250.0 最终验证后的发布候选盘点为 63 个受版本控制文件、1,396,584 字节（约 1.33 MiB）；非 `.git` 工作区与之相同。2026-08-31 提交前快照中的 `.git` 为 688 个文件、8,015,623 字节，整个项目目录合计 751 个文件、9,412,207 字节（约 8.98 MiB），低于 10 MiB，也远低于 500 MB 监视阈值。精确 `.git` 字节会随暂存、提交与维护记录正常变化，不作为发布契约。项目中没有 `node_modules`、coverage 历史、日志、截图集、录屏或构建包；本版只修改既有运行时代码、测试、版本与发行文档，没有新增运行时模块、依赖、锁文件、手工发布包或临时浏览器产物。

### `D:\Agent Workspaces\Agent Tools\reentry-deck`

拟放入可编辑的完整 Git 仓库，不拆散历史：

- `.git/`：本地提交历史、分支和仓库元数据。
- `.github/workflows/ci.yml`：只读验证工作流；不含 secrets、制品、部署或发布能力。
- `src/`、`index.html`、`sw.js`、`app.webmanifest`、`assets/icon.svg`：应用源码与静态运行资产。
- `tests/`：Node 原生测试套件。
- `tools/server.mjs`：零依赖本地 HTTP 服务。
- `package.json`、`.gitignore`、`.gitattributes`：运行、检查与版本控制文本配置。
- `README.md`、`PRODUCT.md`、`CHANGELOG.md`、`docs/`：产品、架构、变更和 QA 文档。

建议迁移单位：整个当前仓库目录，而不是逐文件移动；等待用户批准后再执行。

### `D:\Agent Workspaces\Agent Delivery\reentry-deck`

拟放入交付与交接材料：

- `HANDOFF_DRAFT.md`：本报告草稿。
- `v0.244.0`–`v0.250.0` GitHub Release：使用 GitHub 自动生成的源代码 ZIP / tarball；不在本机生成重复发布包。
- 可选的独立校验清单：当前无需手工发布包，因此不存在也不生成。
- Antigravity 的跨项目评估记录按既定规则保留在 `D:\Agent Workspaces\Agent Delivery\antigravity-subagents-evaluation\threads\01a04403-4ffe-7671-bbe4-50fada95cd7c`，不复制进项目交付目录；其中只含安全指标与审计 sidecar，不含提示词、回答或凭据。

### `D:\Agent Workspaces\Agent Temp\reentry-deck`

当前项目临时目录不存在。0.231.0 收尾时只删除了以下四份很小且可再生成的只读评审合约，没有触及其他临时或交付记录：

- `antigravity/server-failure-review-v1.schema.json`：0.206.0 公共服务器失败契约评审使用的结构化输出 schema。
- `antigravity/host-input-review-v1.schema.json`：0.207.0 公共 HOST 契约评审使用的结构化输出 schema；本次高模型结果因超过输出上限被拒绝，没有作为业务贡献采用。
- `antigravity/workspace-markdown-review-v1.schema.json`：0.219.0 公开提交只读评审的结构化输出 schema；高模型返回无效 JSON，没有可用业务输出，也未采用任何结论。
- `antigravity/dialog-accessibility-review-v1.schema.json`：0.227.0 公开提交对话框与键盘可达性评审的结构化输出 schema；高模型返回无效 JSON，没有可用业务输出，也未采用任何结论。
- 四份 schema 合计 5,826 bytes，均可从对应公开只读评审合约重新生成；删除不经过版本控制，不能直接撤销，但不影响源码、审计 sidecar 或已发布结果。
- 允许：一次性浏览器录制、可再生大截图集、覆盖率临时输出、测试缓存。
- 当前没有待清理项目产物；后续仅在用途和可再生成性均已确认时处理新临时文件。
- 本次不移动或复制任何内容，也不删除源码、用户数据、未提交成果或用途不明的文件。

## 安全维护声明

- GitHub 同步只允许普通快进 push；未强推、重写历史或删除远端分支。
- 不移动或复制源码、用户数据、未提交成果或缓存；0.231.0 已删除的四份可再生成 schema 不会在本轮重建。
- 0.244.0 使用三个边界清楚的 Codex 只读审计：远端核验确认本地、GitHub `main`、注释标签与 0.243.0 Release 完全一致；产品与可靠性审计独立指出项目编辑、状态、轨迹与恢复动作仍在提交前写入通用焦点目标。子任务没有编辑、启动服务、提交或发布；父任务独立完成失败复现、实现、验证、浏览器验收与集成。
- 0.247.0 使用边界清楚的只读候选、实现、测试与文档审计：独立审计确认状态/关闭原因错配会污染周报统计，并发现缺失状态未进入既有活动数量、结束时间、归档项目和结束检查点检查，以及原子性测试未观察瞬时存储调用。子任务没有编辑、启动服务、提交或发布；父任务独立完成失败复现、实现、测试强化、浏览器定向验收与集成。
- 0.248.0 使用三个边界清楚的只读 CI 设计、测试与文档审计，并由父任务以 GitHub 官方文档和 action 官方提交独立复核；审计把移动 runner 标签收紧为 Ubuntu 24.04，并把抽样断言强化为完整工作流骨架。子任务没有编辑、启动服务、提交或发布；父任务独立完成失败复现、实现、验证与集成。工作流尚未进入远端，因此没有虚报 GitHub 绿色结果。
- 0.249.0 使用边界清楚的只读渲染路径、测试与文档审计，独立复核覆盖所有直接重绘、同步外部采用竞态、模态背景草稿和空正文类型，并确认成功内联记录是唯一消费点；终审先发现导入读取完成会替换后来模态，又发现根节点替换到下一帧恢复之间的单帧空窗，以及连续保留型重绘可能让旧帧丢掉待恢复快照。父任务逐项补失败优先契约、请求身份恢复门和对话框内可访问提示后闭合；文档审计还收紧了本地动作焦点与外部选区恢复的承诺。子任务没有编辑、启动服务、浏览器、提交或发布；父任务独立完成失败复现、实现、自动化与真实浏览器验收。浏览器技能用于验证真实 DOM 的同标签与双标签草稿行为；待恢复模态竞态由纯恢复门和源码契约验证，未冒充浏览器实测。验收后关闭全部标签并确认临时端口无监听。
- 0.250.0 使用边界清楚的只读存储风险、撤销/导入界面与对抗时序审计；首版内嵌元数据方案被独立终审证明会让已缓存 0.249.0 页面拒绝新版主数据并反向覆盖，因而在提交前整体改成向旧客户端不可见的侧边账本。后续故障注入又闭合格式标记丢失、空字符串主数据、首次修复中止/成功、同一恢复点双项认可、来源读取瞬断、孤立副本首次配额、写入或删除生效后抛错，以及副本建立、清理和配额删除各阶段的损坏替换。租约终审进一步逐个复现并修复过期释放误删竞争者、配额/来源恢复跨 4.6 或 6 秒、合作胜者在延迟副本/账本/清理期间接管、同所有者续期或恢复令牌写入后抛错，以及令牌写入正常返回或抛错时调用本身耗尽租期；恢复路径只采用仍有安全余量的精确同所有者令牌，否则用刷新时钟做有界恢复租约重取和完整事实复核。合法胜者只按自己的账本或唯一可归因直接后继事实补回实际前版；最终释放不删除过期、临近过期或外部令牌。父任务逐项补失败优先回归、重复身份复核、事实重读恢复、条件清理和真实降级措辞；Web Storage 无 compare-and-swap、单次调用跨完整租期与最终读到变更调用之间的残余边界被明确保留，未冒充跨键原子保证。子任务没有编辑、启动服务、浏览器、提交或发布；父任务独立完成实现、自动化、文档与集成。
- 当前持续维护流程已获普通 GitHub 同步授权；仍不执行强推、历史重写、远端分支删除、用户数据处理或未经批准的资产迁移。
