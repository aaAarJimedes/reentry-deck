# 版本记录

本项目遵循“可运行里程碑优先”的持续迭代方式。

## 0.232.0 · 2026-08-30

### 离线外壳不再被单文件网络响应改写

- 在线导航与模块继续采用网络优先，新鲜 200 响应立即交付；它们不再克隆并写回已经原子安装的版本缓存。
- 已安装缓存现在始终代表一套完整发布：即使在线期间碰到下一次部署的部分文件，随后断网也只回退安装时彼此一致的文档与模块，不会拼出跨版本外壳。
- 移除不再需要的后台缓存刷新与 fetch 生命周期聚合；缓存打开仍与网络并行并受 750ms 上界保护，404/5xx、网络超时、缓存拒绝和客户端取消语义保持不变。

### 验证

- 服务工作线程定向测试 13 项通过；新增导航与模块的“在线新正文、离线旧版本”回归路径，并继续覆盖原子安装、激活清理、HTTP 失败、缓存拒绝/停滞、网络超时和请求取消。完整自动化、静态检查与浏览器发行验收见本版交接记录。

## 0.231.0 · 2026-08-29

### 本机数据保护结果只通过一个实时通道播报

- 数据保险箱的可见保护说明不再同时声明第二个 `role="status"`；手动请求保护时，同一句结果不会与通用辅助反馈重复播报。
- 可见文字及其稳定 ID 完整保留，操作按钮继续用 `aria-describedby` 读取当前状态；手动结果仍由通用 `polite` / `atomic` 区可靠播报，并保留静默的同文视觉 toast。
- 启动后的只读保护检查若在数据保险箱完成，会在重绘可见说明后显式写入同一个通用实时区。最新请求获胜、销毁失效与路由约束不变，搜索和项目筛选的局部状态不受影响。

### 验证

- 界面定向契约 57 项、完整 397 项测试与全部静态检查通过。真实浏览器确认保护说明没有 `role` / `aria-live`，按钮仍引用它；启动检查与手动请求后的可见说明、静默视觉 toast 和唯一通用播报文字一致，焦点保留在操作按钮，页面无横向溢出且运行诊断为空。

## 0.230.0 · 2026-08-29

### 旧界面的快捷命令不再跨重绘执行

- 命令面板关闭后延迟到下一帧执行的动作，现在会绑定用户点击时的渲染世代；跨标签同步、时间边界刷新或其他重绘若先发生，旧面板留下的回调会直接失效。
- 世代在关闭对话框之前取得，并与应用销毁共同构成提交守卫；正常下一帧仍通过共享用户动作边界执行，不改变错误反馈、命令可用性或键盘入口。
- 直接按钮、键盘快捷键、路由和数据事务均不改变；修复只收紧旧 DOM 发起的八类延迟命令，避免它们作用于已经更新的工作区。

### 验证

- 界面定向契约 57 项、完整 397 项测试与全部静态检查通过。真实浏览器从搜索面板执行“数据保险箱”：对话框关闭并在下一帧进入正确页面；重新打开搜索时焦点仍落在搜索框，模态边界正常，页面无横向溢出且运行诊断为空。旧世代拒绝条件由定向契约锁定。

## 0.229.0 · 2026-08-29

### 同一帧内只提交最新操作播报

- 通用辅助反馈新增实例内单调序号；连续动作在浏览器下一帧提交消息时，较早请求会发现自己已经过期并直接退出。
- 序号在查找实时节点之前就推进，因此新页面暂时缺少目标节点时也会使旧请求失效；应用销毁同样推进序号，所有待执行播报帧都失去提交资格。
- 最终写入仍要求实例存活、请求最新且原节点保持连接。单通道 toast 去重、搜索/筛选局部状态、可见反馈、文字边界和焦点行为均不改变。

### 验证

- 界面定向契约 57 项、完整 397 项测试与全部静态检查通过。真实浏览器连续切换“减少动效 → 跟随系统”：最终通用状态只保留最新的“动态效果已改为跟随系统”，按钮状态和焦点同步落在最终选择，实时区继续为 `polite` / `atomic`，页面无横向溢出且运行诊断为空。

## 0.228.0 · 2026-08-29

### 可见反馈只通过一个实时通道播报

- 可见 toast 区域不再与隐藏状态区同时声明 `aria-live`；保存、复制、下载和设置动作不会再让辅助技术连续听到两份相近的成功消息。
- 通用操作反馈统一使用礼貌且原子化的隐藏实时状态。只有 toast 的反馈会自动把同一份 500 字符有界文本送入该通道，错误和普通成功仍然可访问。
- 已经提供专用简短播报的动作会保留那句更聚焦的说明，并让随后出现的视觉 toast 静默；视觉内容、持续时间、最多四条上限、错误颜色和焦点行为不变。

### 验证

- 界面定向契约 56 项、完整 396 项测试与全部静态检查通过。真实浏览器确认仅有 toast 的示例载入正常播报；开始会话保留详细视觉 toast，但通用状态只读出“会话已开始”，页面无横向溢出且控制台为空。

## 0.227.0 · 2026-08-29

### 搜索读屏反馈只播报结果摘要

- 全局搜索不再把最多 40 条可交互结果组成的整个列表设为实时区域；连续输入时，辅助技术只接收简短、礼貌且原子化的状态更新。
- 持久的隐藏状态节点会明确播报快捷动作已显示、无匹配、精确命中数，以及结果被截到前 40 条时的显示数量；可见列表、链接顺序和键盘焦点保持不变。
- 每次查询仍只执行一次有界索引搜索；同一个投影同时返回安全 HTML 与播报摘要，不为无障碍反馈重复扫描工作区或建立第二份结果数组。

### 验证

- 搜索与界面定向契约 71 项、完整 395 项测试与全部静态检查通过。真实浏览器依次输入命中词、无结果词并清空：状态准确切换，焦点始终留在搜索框，结果列表无实时属性、页面无横向溢出且控制台为空。

## 0.226.0 · 2026-08-29

### 工作流对话框读出可见说明

- 快捷记录、开始会话、可靠检查点、编辑项目和全局搜索五个工作流对话框，现在都把标题下方的可见指导语绑定为原生对话框的无障碍描述。
- 说明节点直接复用现有文字，不添加隐藏副本、额外状态或新的交互分支；视觉布局、表单内容、焦点策略和提交行为保持不变。
- 新建项目、快速复核、归档确认和导入预览既有说明关联继续保留；新增契约测试逐一锁定五个补齐入口，避免模板演进时丢失读屏上下文。

### 验证

- 定向界面契约 54 项、完整 394 项测试与全部静态检查通过。真实浏览器逐一核对五个说明目标，并实际打开快捷记录与搜索：标题和说明正确、首个业务控件获得焦点、页面无横向溢出且控制台为空。

## 0.225.0 · 2026-08-29

### 项目交接明确现场新鲜度

- 纯文本复航简报与 Markdown 现在都在项目状态后写出现场更新时间；内容被转发、重命名或移出复航台后，读者仍能判断这份证据来自何时。
- 时间只接受精确 24 字符、可按毫秒 UTC ISO 原样往返的持久化值；缺失、非规范日期、偏移格式和任何对象都稳定显示“时间未知”，不会调用不可信的字符串转换钩子。
- 两种出口继续共用同一次有界字段投影；新增一行不改变状态、未决余量、证据缺口、Markdown 转义、下载名或工作区数据。

### 验证

- 自动化逐字核对两种格式中的规范时间，并用抛错 `toString` 对象确认零强制转换；完整 393 项测试与全部静态检查通过。真实浏览器复制并下载示例项目简报，反馈可见、焦点保留、项目仍为推进中、页面无横向溢出且控制台为空。

## 0.224.0 · 2026-08-29

### 项目交接脱离界面后仍说明状态

- 复制的纯文本复航简报与下载的 Markdown 现在都在标题后明确写出项目状态：推进中、暂泊、受阻或已归档；转发到聊天、笔记或版本库后不再依赖原按钮名称理解生命周期。
- 两个出口继续共享同一次有界字段投影和同一状态映射；未知防御性输入稳定显示“状态未知”，不执行字符串强制转换，也不改变工作区。
- 归档详情因此在导出正文中保留“已归档”语义，普通详情和活动会话也使用相同格式；证据状态、精确余量、Markdown 转义及长度上界保持不变。

### 验证

- 自动化逐字核对受阻纯文本与已归档 Markdown，并覆盖未知状态回退；三处旧测试从固定行号改为按“未决事项”字段定位，完整 393 项测试通过。真实浏览器归档示例项目后复制并下载，反馈可见、焦点保留、状态仍为归档、页面无横向溢出且控制台为空。

## 0.223.0 · 2026-08-29

### 时钟回拨时备份名称仍与正文一致

- 存储层新增唯一的有效备份时间出口：先把用户动作时间钳制到不早于已提交工作区更新时间，再返回可供信封和下载名共享的有限数值时间戳。
- 对象备份、紧凑文本备份与 JSON 下载文件名现在全部消费该出口；系统时钟回拨到前一天时，规范 UTC `exportedAt` 与本地日期文件名不再因为单调工作区锚点而跨日分离。
- 正常时钟下输出字节与文件名语义不变；紧凑状态原文、校验码、导入兼容、一次 Blob 和对象 URL 回收路径均不改变。

### 验证

- 新增确定性回拨测试，把工作区更新时间固定在系统动作时间之后，确认共享时间、对象信封和文本信封都钳制到同一毫秒；UI 契约锁定文件名消费同一值，完整 393 项测试通过。真实浏览器生成 JSON 后成功反馈可见、焦点保留、页面无横向溢出且控制台为空。

## 0.222.0 · 2026-08-29

### 备份正文与文件名共享同一时刻

- 完整 JSON 备份现在在用户点击时只读取一次系统时间，并把同一个有限数值时间戳同时传给信封 `exportedAt` 和本地日历文件名。
- 即使导出恰好跨过本地午夜，文件名日期也不会与备份正文记录的生成时刻分属两天；规范 UTC 元数据、本地日期展示和严格下载名投影各自语义保持不变。
- 改动继续复用紧凑规范状态原文和单次 Blob 出口，不新增状态扫描、持久化字段、数据副本或对象 URL 生命周期。

### 验证

- 自动化锁定导出动作只有一次 `Date.now()`，正文与文件名都消费该快照；目标测试 129 项通过。真实浏览器从数据保险箱生成 JSON，成功反馈可见、焦点保留、页面无横向溢出且控制台为空。

## 0.221.0 · 2026-08-29

### 进行中的会话也能随时带走简报

- 活动会话展开后，工作面板现在保留复制复航简报和下载 Markdown；不必先留下检查点、快速停靠或结束计时才能把当前现场带到笔记、聊天或版本库。
- 工作面板消费主渲染已经构建的同一张复航卡和共享导出工具，不新增项目证据扫描、历史数组、DOM 工具实现或异步状态分支。
- 复制与下载仍是纯只读操作，继续使用项目上下文可访问名称、剪贴板最新请求门、本地日期文件名与对象 URL 回收；活动会话、输入草稿和计时器保持原状。

### 验证

- 自动化锁定活动分支显式接收当前卡片并调用共享导出工具；真实浏览器开始一段示例会话后成功复制、下载，焦点保留且计时器继续从 9 秒推进到 11 秒，页面无横向溢出、控制台为空。

## 0.220.0 · 2026-08-29

### 归档项目无需恢复即可带走简报

- 归档项目的只读详情在“最后的复航现场”直接提供复制简报和下载 Markdown，不再要求先恢复项目、改变状态或重新进入舰桥。
- 活动与归档详情共用同一个导出工具渲染器、同一项目 ID 和同一有界复航卡；归档入口使用明确的上下文可访问名称，并继续显示证据完整度。
- 两项操作只读取当前本机快照，复用既有剪贴板请求门、Markdown 转义、本地日期文件名和对象 URL 生命周期；归档项目仍不接受会话、轨迹或其他写入。

### 验证

- 自动化锁定活动与归档详情都消费共享导出工具，归档分支不调用恢复逻辑；真实浏览器归档示例项目后，从归档舱打开详情并成功复制、下载，焦点保留、状态仍为归档、页面无横向溢出且控制台为空。

## 0.219.0 · 2026-08-29

### 导出文件名跟随本地日历日期

- JSON 备份、项目 Markdown 和工作区 Markdown 不再从 UTC ISO 时间截取日期；在东半球本地零点后的若干小时或西半球本地午夜前，文件名现在与用户看到的本地日期一致。
- 三类导出共用一个严格日期投影，只接受有限数值时间戳并拒绝字符串、容器、无穷值与超出 `Date` 范围的数字；年月日使用本地日历 getter 和固定补零，不依赖地区化文本解析。
- 文件内容里的业务时间继续保持规范毫秒 UTC，便携文件名过滤、下载生命周期、工作区状态和备份格式均不改变。

### 验证

- 自动化在 `Asia/Shanghai` 固定 `2026-01-01T16:05:00Z`，确认文件名日期为本地 `2026-01-02`，并覆盖无效输入及三个 UI 调用点；真实浏览器逐一触发工作区 Markdown、项目 Markdown 和 JSON 备份，反馈与焦点正常、页面无横向溢出且控制台为空。

## 0.218.0 · 2026-08-29

### 整个工作区可以下载 Markdown 交接清单

- 项目舰桥在复制交接清单旁新增可访问的“下载 Markdown”；文件保留当前会话、前五个优先项目、下一动作、前四个关注项目、原因余量和七日标量，适合直接放入交接目录或版本库。
- Markdown 出口复用原有有界工作区交接投影，不新增项目、会话或历史扫描；所有现场内容在进入标题、列表、强调和引用结构前统一转义，包括容易触发 GFM 删除线的波浪号。
- 下载继续复用既有 UTF-8 Blob、跨平台文件名投影和对象 URL 生命周期；首页双按钮允许换行，在当前浏览器宽度下不产生横向溢出，也不修改工作区或备份格式。

### 验证

- 自动化覆盖恶意链接、HTML、强调、删除线、隐藏关注原因、周度指标、固定输出上界、可访问名称和下载命名；真实浏览器载入示例后成功触发工作区 Markdown 下载，焦点留在原按钮、成功播报可见、页面无横向溢出且控制台无错误。

## 0.217.0 · 2026-08-29

### 三项关注原因不再冒充完整诊断

- 每个关注项目继续只保留前三项主要原因，但同一次评分会精确记录全部适用原因数；窗口外还有活动会话、阻塞、问题、检查点、下一步或离开时长风险时，页面明确显示“另有 N 项现场缺口”。
- 复制的工作区交接清单消费同一完整总数，并为未列出的原因保留尾部提示；旧投影缺少总数时按现有原因数组安全降级，畸形大数组也只读取前三项正文。
- 精确总数只增加一个标量，不扩大关注堆、项目窗口、最终排序、DOM、交接输入扫描或复航证据索引；产品迭代池同时标记项目 Markdown 简报已经完成。

### 验证

- 自动化构造同时命中七类关注条件的项目，验证原因总数为七、正文窗口固定为三，交接文本披露四项余量；真实浏览器把示例阈值改为一天后显示第四类原因余量，整条链接可访问名称同步包含提示且控制台无错误。

## 0.216.0 · 2026-08-29

### 复航卡可以下载本地 Markdown 简报

- 项目复航卡在“复制简报”旁新增带项目可访问名称的“下载 Markdown”；文件包含当前状态、第一动作、实时未决事项、复航提示、证据完整度和核对声明，适合直接放入项目目录或版本库。
- Markdown 与纯文本复制共用同一份有界字段投影和三项未决证据窗口，不额外扫描项目历史；用户内容中的链接、HTML、标题、强调和代码标记会先转义，避免意外改变文档结构。
- 下载复用既有跨平台文件名投影、临时对象 URL 延迟回收和同步用户操作错误边界；文件保持 UTF-8 文本、零运行时依赖，也不改变工作区或备份格式。

### 验证

- 自动化核对危险 Markdown 标记、实时未决优先级、精确隐藏数量、Unicode/长度上界、文件 MIME/命名调用和上下文可访问名称；真实浏览器载入示例项目后成功触发下载反馈，按钮名称包含项目名且控制台无错误。

## 0.215.0 · 2026-08-29

### 大量中断会话不再扩大复航卡对象窗口

- 复航卡在既有项目会话单遍中精确累计检查点后的全部未收拢或中断会话，但只保留最新三项兼容证据对象；完整度扣减、复航缺口和页面警告改为消费精确总数。
- 正常单一活动会话的对象与提示语义保持不变；大量历史中断会话不再为一个仅展示数字的警告建立等量附加数组，旧卡片缺少总数字段时仍按现有数组安全降级。

### 验证

- 自动化在一个项目、一个检查点和 49,998 段中断会话组成的 50,000 记录边界验证：总数精确、证据对象窗口固定为三项、后写入优先、完整度扣减封顶且复航缺口显示完整数量。

## 0.214.0 · 2026-08-29

### 两条最近决定会披露更早数量

- 复航卡在既有项目证据单遍中同时累计全部决定，只保留最新两条正文；项目还有更早决定时明确提示窗口外数量，不再需要从统计数字猜测历史深度。
- 详情卡的决定统计复用同一个完整计数，不再在单次轨迹遍历中维护两个独立计数分支；正文数组、源扫描、排序和 DOM 继续保持有界。

### 验证

- 自动化以 49,999 条同毫秒决定验证精确总数、两项正文窗口和后写入优先顺序；带统计复航卡继续与独立项目统计完全一致，界面契约覆盖较早决定余量说明。

## 0.213.0 · 2026-08-29

### 检查点后变化会披露完整数量

- 复航卡在既有项目证据单遍中同时累计检查点后的全部随记、发现、决定和下一步变化，只保留最新三条正文；窗口外还有变化时明确提示隐藏数量。
- 没有可靠检查点时，变化窗口继续覆盖项目现有的可恢复轨迹；精确总数只增加一个标量，不扩大正文数组、证据扫描、排序或 DOM。

### 验证

- 自动化以 49,999 条全部位于检查点后的同毫秒轨迹验证精确总数为 49,999、正文仍为最新三条且后写入者优先；界面契约验证余量说明消费完整总数。

## 0.212.0 · 2026-08-29

### 三条置顶航标会披露完整数量

- 复航卡在既有项目证据单遍中同时累计全部置顶航标，只保留最新三条正文；窗口外还有航标时会明确提示隐藏数量，不再让有界列表看起来像完整集合。
- 精确总数只增加一个标量，不扩大置顶数组、项目证据扫描、排序或 DOM；缺少新字段的旧投影继续按现有三条窗口安全显示。

### 验证

- 自动化以 49,999 条全部置顶的同毫秒轨迹验证精确总数为 49,999、正文仍为最新三条且后写入者优先；界面契约验证余量说明消费完整总数。

## 0.211.0 · 2026-08-29

### 搜索会区分完整命中与四十项窗口

- 全局搜索新增兼容的窗口投影，在既有候选评分单遍中同时返回精确命中总数和最多四十项排序结果；原有数组 API 保持不变。
- 命中超过窗口时，搜索面板改为显示“找到 N 条匹配，显示前 40 条”，不再把有界结果数组长度冒充全部证据数量。
- 精确计数只增加一个标量，不扩大搜索堆、最终排序、DOM 或源候选读取；`limit: 0` 的窗口调用可以只计数而不保留任何结果，旧数组调用仍立即返回空数组。

### 验证

- 自动化以 50,000 个索引候选验证 49,999 个宽泛命中只物化和排序四十项，同时精确返回总数；普通三项命中、单项窗口、零项计数窗口、无效查询和旧 API 行为继续通过。

## 0.210.0 · 2026-08-29

### 四项关注窗口不再隐藏其余风险项目

- 工作区共享概览在既有关注评分单遍中同时累计所有存在现场缺口的项目数；界面仍只保留最值得核对的四项，但会明确提示窗口外还有多少项目。
- 复制的工作区交接清单消费同一精确总数，在四项文本窗口后补充“另有 N 个项目未列出”；即使调用方只提供总数而没有可展示卡片，也不会错误声称没有明显缺口。
- 总数累积不扩大关注堆、最终排序、DOM 或交接清单输入扫描，零项窗口和缺失旧字段继续保持兼容行为。

### 验证

- 自动化以 1,000 个均需核对的项目验证精确总数、四项关注窗口、单次证据索引和有界最终排序；交接文本同时覆盖有窗口余量、仅有总数与旧调用缺少总数字段的分支。

## 0.209.0 · 2026-08-29

### 复制简报会披露窗口外未决事项

- 项目复航简报读取完整未决汇总，在正文窗口之外仍有问题或阻塞时附加“另有 N 条未显示”，不再让复制到聊天、邮件或笔记的三条摘要看起来像全部事实。
- 800 字符未决行会先为余量提示保留尾部预算，再把最多三条实时信号公平压入剩余空间；即使第一条接近字段上限，后续信号和精确余量也仍有可见位置。
- 旧卡片或独立调用缺少完整汇总时继续兼容，并只读取生产者约定的三项窗口；计数不会扩大源扫描、复制文本或 Unicode 风险边界。

### 验证

- 自动化核对 50,000 条声明总量只读取前三项并报告 49,997 条余量；三条 1,200 字符级信号仍在 800 字符内各有有界摘要、保留六条余量提示且不拆分 Unicode。

## 0.208.0 · 2026-08-29

### 三条摘要不再冒充全部未决事项

- 复航卡仍只展开最近三条问题/阻塞正文，但同一次证据扫描会同时保留全部未决问题数、阻塞数和各自最早时间，不再用显示窗口代替真实总量。
- 项目页在窗口外还有证据时明确显示“另有 N 条”，引导到完整轨迹；首页受阻项目的推荐理由和关注清单也改用完整计数。
- 关注原因会给出最早未解决阻塞的整天年龄，项目有大量信号时评分仍封顶、正文与 DOM 仍保持固定上界。

### 验证

- 自动化用九条未决信号和一条已解决阻塞核对完整 5/4 分类、最早 10/5 天、三条正文窗口、剩余提示、精确推荐理由和有界关注排序；50,000 条边界投影继续通过。

## 0.207.0 · 2026-08-29

### 监听主机在联网前先过边界

- `HOST` 现在会在 DNS 与 `listen()` 前完成语法和容量校验；空白、控制字符、协议、凭据、端口、路径、畸形括号、DNS 空标签或超长标签不再落入延迟解析和内部堆栈。
- ASCII / Punycode 主机名、规范 IPv4、裸 IPv6 与显式通配地址保持可用；带方括号的 IPv6 会规范成 Node.js 监听需要的裸地址，再由启动提示安全补回方括号。
- 主机值不做静默 trim，空字符串不会意外变成所有接口监听；Unicode 域名需先使用 ASCII Punycode，带 zone 的链路本地 IPv6 保守拒绝。

### 验证

- 自动化覆盖 loopback、大小写与尾点、Punycode、IPv4 / IPv6、通配监听、凭据/URL/路径、Unicode 空白、数值地址歧义、63/253 字符 DNS 边界和畸形 IPv6；独立 CLI 子进程确认坏主机在 DNS 前以状态 1 和单行说明退出。

## 0.206.0 · 2026-08-29

### 本地服务启动失败现在可直接处理

- 端口已占用时，终端会显示包含主机与端口的一行提示并以非零状态退出，不再落入 Node.js 原始堆栈。
- 监听权限不足获得独立、可操作的说明；其他未知启动错误继续保留完整堆栈，避免为了简洁而丢失真正故障的诊断证据。
- 启动错误监听只覆盖 `listen()` 阶段，成功绑定后立即移除；正常地址提示、`PORT=0`、静态白名单和零运行时依赖保持不变。

### 验证

- 自动化真实占用一个随机本地端口，再启动独立 CLI 子进程，核对状态 1、空标准输出、精确单行错误和零堆栈；单元测试另覆盖 `EACCES`、未知错误完整堆栈及异常错误访问器。

## 0.205.0 · 2026-08-29

### 自动端口提示现在可以直接使用

- 以 `PORT=0` 启动时，本地服务器会读取操作系统最终绑定的端口并显示真实 URL，不再错误提示不可访问的 `:0`。
- 启动地址统一使用监听结果而不是原始请求值；IPv6 地址会自动加方括号，畸形主机或端口则不伪造可点击 URL。
- 默认 `127.0.0.1:4173`、显式端口、零依赖静态白名单和全部安全响应头保持不变。

### 验证

- 单元测试覆盖实际端口优先、IPv4、裸/已括起的 IPv6、可信回退和无效地址拒绝；真实以 `PORT=0` 启动后获得 `http://127.0.0.1:12267`，直接打开应用成功且控制台无错误。

## 0.204.0 · 2026-08-29

### 现有项目可以成为安全的起步模板

- 活动与归档项目详情都新增“复用为新项目”：名称、目的说明、第一动作和识别颜色会带入可编辑的新建表单，原项目的会话、轨迹、检查点、状态和时间绝不复制。
- 模板名称在 100 字符模型边界内追加“新现场”并完整保留 Unicode；草稿是冻结的独立投影，只扫描目标项目，不读取历史集合。
- 对话框明确说明复制边界并全选建议名称；取消模板后普通新建会恢复空白，同时保留原有普通草稿的关闭后续写体验。
- 所有原生对话框的非冒泡 `cancel` 事件改在捕获阶段清理暂态上下文，Escape 关闭不再遗留导入、编辑、归档、检查点或模板状态。

### 验证

- 自动化覆盖活动/归档来源、最大长度组合 emoji、历史集合零读取、冻结草稿、模板取消清理和 `cancel` 捕获注册；真实浏览器验证两类入口、表单预填/全选、取消回空白，以及新项目 0 会话、0 检查点、0 决定、0 轨迹，控制台无错误。

## 0.203.0 · 2026-08-29

### 样式策略不再依赖内联豁免

- 本地服务器的 CSP 收紧为 `style-src 'self'`，不再允许 `unsafe-inline`；运行时模板和剪贴板回退也不再写入 `style` 属性或 CSSOM 内联样式。
- 复航完整度环改用同源样式表控制的 SVG，并用有界 `stroke-dasharray` 展示动态百分比；六种识别颜色与静态间距全部改为固定类名，视觉和可访问名称保持不变。
- 发布契约会同时阻止模板内联样式、运行时 `.style` 写入、服务器内联样式豁免和四方版本漂移进入发布。

### 验证

- 自动化覆盖自托管样式策略、剪贴板回退类名和严格响应头；真实浏览器验证 0%/100% 复航语义、六种可见色块与空控制台，未出现 CSP 拒绝或脚本错误。

## 0.202.0 · 2026-08-29

### PWA 清单进入发布契约

- 发布测试现在真实解析 `app.webmanifest`，并核对启动 URL 位于同源 scope、显示模式为独立应用、文档语言与主题色一致。
- 每个清单图标必须保持同源、位于应用 scope 内，同时存在于零依赖 HTTP 白名单和原子离线外壳；外部、越界或仅在线可见的安装资源会阻止发布。
- 继续保留相对 `start_url` / `scope` 与规范默认安装身份，避免为可部署在根路径或任意子路径的应用强行固定不兼容的绝对 `id`。

### 验证

- 新增清单解析、URL 解析、HTML 元数据、图标 MIME/用途、HTTP 与离线缓存交叉验证；0.201.0 的四方版本绑定和所有运行时模块递归可达性检查继续通过。

## 0.201.0 · 2026-08-29

### 离线壳版本不再漂移

- 服务工作线程的 `BUILD_ID` 改为与应用语义版本完全一致；每个发布版本都会建立新的原子离线缓存世代，而不是依赖维护者同时更新另一套日期编号。
- 发布契约现在把 package 版本、备份 `APP_VERSION`、最新 CHANGELOG 和离线构建标识四方绑定，任一遗漏都会在提交前失败。
- 仓库明确把 JavaScript 与 ESM 源码固定为 LF，避免 Windows 自动换行策略让静态结构契约在干净检出后误判或批量失效。
- 运行时仍保持 network-first 与完整缓存回退；本次只收紧发布边界，不改变在线请求、缓存清理或现有数据存储语义。

### 验证

- 发布契约先读取静态可审计的 `BUILD_ID` 并与 0.201.0 精确比较；Windows CRLF 复现后由仓库属性恢复稳定 LF，完整服务工作线程生命周期测试继续覆盖原子安装、旧壳清理、网络超时、取消、配额与离线失败路径。

## 0.200.0 · 2026-08-29

### 页面缓存恢复也会核对工作区

- 浏览器从后退/前进缓存恢复页面时，通过 `pageshow.persisted` 主动核对实际持久化工作区，覆盖页面冻结期间没有收到可见性切换或跨标签事件的恢复路径。
- 首次载入的普通 `pageshow` 不触发重复读取；只有浏览器明确报告缓存恢复时才复用 0.199.0 的有界本地同步与计时器校准。
- 可见性恢复与页面缓存恢复共享单一刷新边界，外部状态采用、冲突保护、暂态草稿保留和无网络/无写入属性保持一致。

### 验证

- UI 合约锁定 `pageshow` 只接受 `persisted` 恢复，并确认两条恢复路径都复用“重读存储 → 刷新计时器”的顺序；浏览器导航验证覆盖返回应用后的可操作页面和表单值恢复，干净加载交互无应用错误。

## 0.199.0 · 2026-08-29

### 返回前台时补偿标签页同步

- 页面从后台恢复可见时主动重读本地工作区，弥补移动端冻结、后台节流或页面恢复期间遗漏 `storage` 事件造成的陈旧界面。
- 补偿读取继续复用既有外部更新路径：只有持久化内容实际变化时才采用新状态，并保留有效对话框、输入草稿、选择、滚动位置与焦点。
- 同步先于计时器校准执行；无变化时只承担一次有界本地读取，不产生写入、网络请求或额外重绘。

### 验证

- UI 合约锁定“恢复可见 → 重读存储 → 刷新计时器”的顺序，并确认外部来源仍通过暂态输入保护渲染；多标签浏览器验证覆盖外部更新采用与对话框草稿保护。

## 0.198.0 · 2026-08-29

### 延迟保护检查不再打断输入

- 启动时的只读持久保护检查即使被宿主延迟，完成后也通过现有暂态对话框保存/恢复边界更新数据保险箱。
- 用户已经打开的新项目、搜索、快捷记录或其他有效对话框会保留输入、选择、滚动位置与焦点；上下文已经失效的对话框仍按既有规则拒绝恢复。
- 非数据保险箱路由继续只更新实例内状态，不产生额外页面重绘；主动请求的即时反馈路径保持不变。

### 验证

- UI 合约锁定保护状态完成使用 `render({ preserveDialog: true })`；既有暂态值上限、上下文校验、外部刷新草稿保护与销毁隔离测试继续通过。

## 0.197.0 · 2026-08-29

### 部分浏览器能力不再误判

- 自动检查缺少 `persisted()`、但仍可能提供 `persist()` 时，状态改为“无法自动确认”而不是“浏览器不支持”；主动请求按钮保持可用。
- `navigator.storage` 整体缺失仍明确归为不支持，敌意 getter 或异步拒绝仍归为检查失败，避免把真实能力缺口与检查接口缺口混为一谈。
- 核心布尔能力执行器允许每个调用点指定“方法缺失”投影：主动请求保持不支持，纯检查使用未知状态，其余接收者、单读和显式布尔边界不变。

### 验证

- 覆盖“仅有 `persist`”返回未知且仍可操作、存储管理器整体缺失返回不支持，以及检查异常返回失败；UI 合约锁定未知状态的主动请求文案。

## 0.196.0 · 2026-08-29

### 启动即恢复真实保护状态

- 应用启动后通过只读 `navigator.storage.persisted()` 检查当前来源是否已经受保护；数据保险箱不再在每次刷新后误回到“尚未检查”。
- 首屏检查期间按钮明确显示“正在检查”并暂时停用；已有授权、尚未授权、不支持或检查异常随后映射到 0.195.0 的同一有限状态和备份建议。
- 状态检查与主动授权复用同一个最后请求获胜门：检查不会弹出权限请求，较晚的用户操作优先，销毁后的完成不会触发重绘。
- 核心能力执行器同时覆盖 `persist` 与 `persisted`，两者共享单次属性读取、正确接收者、显式布尔授权和异常吸收边界。

### 验证

- 新增只读检查的允许、拒绝、伪真值、缺失能力、敌意 getter 和异步拒绝测试；UI 合约锁定启动检查、设置页限定重绘和共享请求门。

## 0.195.0 · 2026-08-29

### 可核对的本机数据保护

- 数据保险箱新增“检查并请求保护”入口，把浏览器结果明确区分为已保护、未授予、不支持或检查失败；非授予结果直接提醒保留 JSON 备份，不让渐进增强被误解为恢复保证。
- 自动请求继续保持安静，只有用户主动检查时才更新带 `role=status` 的说明、恢复按钮焦点并播报结果；已保护和不支持状态会停用无意义的重复请求。
- 持久存储能力调用提取为零依赖核心边界：方法只读取一次、保留原接收者、仅接受显式 `true` 授权，并吸收属性 getter、同步调用和异步拒绝异常。
- 重复点击、自动请求与实例销毁共享最后请求获胜门，旧异步结果不会覆盖较新的界面判断或在销毁后更新页面。

### 验证

- 参数化缺失/非函数能力、显式允许/拒绝、真值伪授权、敌意 getter、同步抛错和异步拒绝；UI 合约核对可访问状态、动作分派、请求门和成功判定。

## 0.194.0 · 2026-08-29

### 恢复路径同享持久存储保护

- 新建项目之外，从备份导入、载入示例和撤销恢复工作区也会在本地事务成功后尽力请求浏览器持久存储，降低这些入口建立的现场被自动清理的风险。
- 持久存储仍是无提示、可失败的渐进增强：拒绝或不支持时不改变保存结果，JSON 导出继续是可靠备份路径。
- 请求严格位于成功提交之后；导入预览过期、导入失败和没有可恢复快照时不会错误触发。

### 验证

- UI 合约锁定四个用户建立工作区入口，并核对每次请求都发生在存储事务之后；既有导入回滚、撤销和持久化失败测试继续通过。

## 0.193.0 · 2026-08-28

### 原子界面启动

- `ReentryApp` 把监听器安装、主题订阅、状态订阅、计时器建立和首次渲染纳入同一初始化错误边界。
- 任一步骤抛错时立即调用幂等销毁路径，撤销已经取得的事件、订阅和异步资源，再把原始异常交给静态恢复界面。
- 清理自身若在受限宿主中失败也不会遮蔽真正的启动原因；存储引用缺失时的销毁改为可选访问，部分构造同样可安全回滚。

### 验证

- UI 生命周期合约锁定“初始化尝试 → 首次渲染 → 失败销毁 → 原异常重抛”的顺序，并继续核对所有资源释放原语。

## 0.192.0 · 2026-08-28

### 有界且不可再抛错的诊断投影

- 存储、剪贴板与界面错误边界改用同一诊断投影，只读取一次异常对象的 `message`；抛错 getter、原始值拒绝和缺失消息统一回退到稳定说明。
- 原始诊断最多读取前 1,000 个 UTF-16 单位，再压到 500 单位的可见输出；异常巨型浏览器消息不再为了短提示完整扫描。
- 扫描边界不切断有效代理对，窗口内的孤立高/低代理替换为 U+FFFD，错误提示不会向 HTML 或本地通知队列传播损坏 Unicode。

### 验证

- 用单次计数 getter、抛错 getter、超窗尾标、孤立代理和完整组合 emoji 参数化公共投影；同时由发布契约确认新模块已加入 HTTP 白名单与完整离线外壳。

## 0.191.0 · 2026-08-28

### 完整避让 Windows 设备文件名

- 下载文件名先提取并收敛首个扩展名前的主干，再判断 Windows 设备别名；`CON .json` 这类扩展名前带尾空格的写法不再绕过保护。
- `COM¹`、`COM²`、`COM³` 与对应 `LPT` 上标数字别名纳入设备名集合，避免浏览器下载交给文件系统后出现拒绝、改名或非预期设备解析。
- 普通近似名称如 `COM10.txt` 保持不变；既有路径分隔符、控制字符、Unicode 完整性、160 单位输出和 640 单位扫描预算继续生效。

### 验证

- 参数化经典设备名、扩展名前尾空格、上标数字别名和安全近似名；危险名称统一添加下划线，安全名称不误改。

## 0.190.0 · 2026-08-28

### 深色主题复航卡对比度

- 复航主卡改用固定深森林局部表面 `#173d3a`，不再随全局深色主题把 `--forest` 反转成浅薄荷背景。
- 米白标题、摘要、下一动作、仪表值和半透明辅助文字重新获得设计预期的深色底；主按钮、次按钮和环形仪表无需分叉主题规则。
- 浅色主题原本的主卡背景就是同一深森林色，因此视觉不变；修复只消除深色主题的浅底浅字组合。

### 验证

- 真实浏览器计算样式先复现浅薄荷 `rgb(198,224,213)` 配米白文字的低对比，再锁定主卡局部背景；新增 CSS 合约防止重新改回主题反转变量，并在浏览器重载后复验。

## 0.189.0 · 2026-08-28

### 规范 UTF-8 校验编码

- 流式 FNV-1a 校验器对孤立 UTF-16 高/低代理项按平台 `TextEncoder` 语义编码为 U+FFFD，而不是生成非法代理 UTF-8 字节。
- 完整代理对仍组合成单个补充平面代码点；ASCII、中文、emoji、转义 JSON 和既有备份校验值保持不变。
- 实现继续逐代码点更新哈希，不创建与 25 MB 备份等大的 `Uint8Array`。

### 验证

- 把含孤立高、低代理的原始序列化字符串分别交给共享校验器与原生 `TextEncoder` 参考实现，两者 FNV-1a 值精确一致；既有 JSON 案例继续通过。

## 0.188.0 · 2026-08-28

### 单一 Unicode 完整性原语

- 孤立代理项检测提升为模型层公开纯函数，持久化校验、Clipboard 入口和下载文件名共同复用，不再维护三份近似实现。
- 原语对非字符串安静返回 `false`，对完整代理对按一个代码点跳过，对单独高/低代理立即报告损坏。
- 统一实现降低未来某个出口遗漏边界修复的风险，不改变上一版已经验证的用户可见行为。

### 验证

- 直接参数化普通文字、完整 `👩‍💻`、单独高代理、单独低代理、夹在正文中的高代理和非字符串；并重跑持久化、复制、下载既有回归。

## 0.187.0 · 2026-08-28

### Unicode 安全浏览器出口

- 下载文件名投影会跳过调用者原本就携带的孤立高/低代理项，不只防止扫描窗自身切断代理对。
- Clipboard 入口在 64 KiB 原始长度门之后检查 UTF-16 完整性，损坏文本不会进入异步剪贴板或隐藏文本域。
- 完整 emoji、组合 emoji、换行和普通交接文字保持可复制；浏览器能力仍只在全部内容前置检查通过后读取。

### 验证

- 文件名混入孤立高/低代理后得到 `badname.json`；复制分别传入两种孤立代理均在能力 getter 前拒绝，完整 `👩‍💻` 仍成功写入 Clipboard API。

## 0.186.0 · 2026-08-28

### 有界备份元数据

- 备份信封 `appVersion` 新增 320 单位原始扫描上限，超长元数据直接忽略，不先执行空白、不可见控制和 Unicode 完整性扫描。
- `exportedAt` 在 `Date.parse` 前要求标准 ISO 毫秒时间的精确 24 字符形状，任意长或非规范日期不进入解析器。
- 应用导出的短版本号与规范 `YYYY-MM-DDTHH:mm:ss.sssZ` 时间保持原样；元数据无效仍不妨碍经过校验的数据主体导入。

### 验证

- 分别传入 321 字符版本号和 321 字符日期串，来源元数据稳定降级为 `null`；既有长但窗内 Unicode 版本标签仍安全压缩到 80 单位。

## 0.185.0 · 2026-08-28

### 超长字段快速拒绝

- 导入文本字段一旦超过对应持久化上限，验证器记录长度错误后不再扫描不可见控制或 UTF-16 代理完整性。
- 已经不可导入的异常长串不会为第二条冗余诊断再执行完整正则/代码单元遍历；上限内字段仍接受全部安全检查。
- ID 验证原本已通过短路 `||` 遵守相同策略，本轮让所有持久化字符串边界一致。

### 验证

- 在 801 字符项目说明后附加双向控制符，并让任何超过 800 字符的正则输入立即抛错；验证只报告长度错误且不触发替身。

## 0.184.0 · 2026-08-28

### 完整 UTF-16 持久化契约

- 文本与 ID 安全校验新增孤立代理项检测，损坏的单独高代理或低代理不能进入项目、会话、轨迹、检查点和元数据引用。
- 检测器按代码单元流式确认代理对，完整 emoji 与组合 emoji 继续允许，不依赖正则后行断言兼容性。
- 这与交接扫描边界的 Unicode 保护形成闭环：新导入先拒绝损坏数据，公开投影也不制造损坏数据。

### 验证

- 项目名称放入孤立高代理、轨迹 ID 放入孤立低代理，同时保留完整 `👩‍💻` 与布局换行；损坏字段被准确报告，合法 emoji 与布局文本不误报。

## 0.183.0 · 2026-08-28

### Unicode 安全扫描边界

- 交接字段截取 4,096 单位证据前缀时会检查最后一个代码单元；若恰为代理对的高代理项，则在规范化前移除。
- 空白密集的畸形输入即使把 emoji 精确跨在扫描边界，也不会在折叠后向简报泄露孤立代理项。
- 普通 BMP 文本、完整 emoji 与 1,200 字符内的合法持久化字段不受影响。

### 验证

- 构造 4,095 个空格后紧跟 emoji 和尾文的摘要，使旧切片精确停在高代理项；生成简报通过孤立高/低代理项正则检查。

## 0.182.0 · 2026-08-28

### 有界交接字段规范化

- 复航简报和工作区交接的文本字段在空白折叠前新增 4,096 单位原始扫描窗，异常长串不会先经历完整正则扫描再截断。
- 字段投影只接受原生字符串，不再调用标题、摘要或下一动作对象的自定义 `toString`；超窗前缀仍以省略号表达信息未完整纳入。
- 持久化模型单字段最高 1,200 字符，正常简报与交接内容完全处于扫描窗内。

### 验证

- 将摘要扩至远超窗口并监测每次 `String.replace` 的接收者长度，同时传入调用即抛错的标题对象；最大正则输入不超过 4,096，转换零调用且简报保持有界可读。

## 0.181.0 · 2026-08-28

### 无强制转换复制入口

- 纯文本复制只接受原生字符串，不再对对象、数组、数字或空值调用 `String(value)`。
- 带副作用或抛错的自定义 `toString` 无法在 64 KiB 长度门之前执行，也不能借强制转换构造隐藏的大载荷。
- 两种内建交接构建器始终返回字符串，异步剪贴板与 DOM 后备路径不受影响。

### 验证

- 传入一个调用即计数并抛错的 `toString` 对象和 `null`，均得到明确类型错误且转换计数保持为零。

## 0.180.0 · 2026-08-28

### 严格记录时间源

- 最底层 `isoNow` 与 `isoAtOrAfter` 不再允许 `Date` 把 `null`、布尔值、数组、对象或空白字符串强制转换为 epoch。
- 项目、会话、轨迹、检查点和事务更新时间现共享同一时间源契约：有限数字、有效 `Date` 或非空有效日期字符串。
- 时钟回拨时取业务锚点最大值的单调语义不变，无效可选锚点仍被忽略。

### 验证

- 三种有效时间形式均精确得到同一 ISO 值；八类无效、容器或非有限输入在两个底层函数中全部得到明确范围错误。

## 0.179.0 · 2026-08-28

### 统一洞察时间门

- 工作区计数、周报、总览和关注甲板共享显式时间解析，只接受有限数字、有效 `Date` 或非空有效日期字符串。
- 周报不再对 ISO 字符串直接做减法而生成 `NaN` 窗口，也不让 `null`、布尔值、数组和对象伪装成 epoch。
- 计数在无效时间下继续安静跳过“今日记录”；需要完整时间窗的周报、总览和关注甲板则抛出一致的业务错误。

### 验证

- 用数字、`Date`、ISO 字符串核对同一个七日窗口，再参数化七类无效或强制转换输入；三个时间窗 API 全部明确拒绝，计数的无效时间降级保持不变。

## 0.178.0 · 2026-08-28

### 严格交接生成时间

- 工作区交接清单不再让 `Date` 隐式把 `null`、布尔值、数组、普通对象或空白字符串解释为 1970 年时间。
- 生成时间现只接受有限时间戳、有效 `Date` 或非空有效日期字符串，与界面其他绝对时间投影使用同一拒绝策略。
- 数字零仍明确代表 Unix epoch，正常默认当前时间与 ISO 字符串输出保持不变。

### 验证

- 参数化覆盖六类强制转换输入并确认全部抛出明确生成时间错误；有效 `Date` 对象仍精确输出预期 ISO 时间。

## 0.177.0 · 2026-08-28

### 文件名视觉安全

- 便携下载文件名过滤扩展到双向覆盖、方向标记、零宽字符、单词连接控制和 BOM，文件扩展名不能被不可见格式字符视觉重排。
- Windows 设备名避让补齐 `CONIN$` 与 `CONOUT$`，并继续覆盖 `CON`、`PRN`、`AUX`、`NUL`、`COM1–9` 和 `LPT1–9`。
- 过滤仍位于 640 单位原始扫描窗和 160 单位输出预算内，对正常日期备份名称零变化。

### 验证

- `report` 与伪装扩展名之间插入 U+202E、尾部插入 U+200B，结果得到可见顺序明确的 `reportgnp.exe`；`CONOUT$.log` 自动前缀下划线。

## 0.176.0 · 2026-08-28

### 有界快捷记录偏好

- 快捷记录项目选择的偏好上下文新增 40 个 ID 读取上限，与最多保留 40 个候选的结果预算一致。
- 偏好排名改为有界索引读取，畸形 50,000 项数组不会再无界扩张 Map；窗口内首个非空、非重复 ID 的稳定顺序保持不变。
- 实际界面只传当前选择、活动会话项目和最近选择三个 ID，用户可见排序没有缩减。

### 验证

- 将有效偏好放在第 40 槽、第 41 槽设读取即抛错 getter、数组长度扩至 50,000；旧项目仍升到首位且不触发窗外 getter。

## 0.175.0 · 2026-08-28

### 输入与输出双预算

- 下载文件名除 160 单位输出上限外，新增独立 640 单位源码扫描窗；超长空白、控制符或禁用字符前缀不能再绕过输出预算迫使完整遍历。
- 纯文本复制把 64 KiB 原始长度检查提前到 `trim()` 之前，超长全空白载荷也直接拒绝，不先执行完整空白规范化。
- 正常文件名和交接文本不受影响；这次补齐的是“过滤后很短、原始输入极长”的容量反例。

### 验证

- 在 640 个空格后放置有效文件名，投影稳定回退且不纳入窗外尾部；65,537 个空格按长度超限而非空内容错误拒绝，浏览器复制依赖仍零读取。

## 0.174.0 · 2026-08-28

### 便携下载文件名

- 本地下载入口新增 160 UTF-16 单位文件名预算，并在创建链接前移除控制符、折叠空白、替换路径与平台禁用分隔符。
- 空结果稳定回退为 `download.json`，Windows 保留设备名加下划线避让；逐码点投影不会截断代理项，也不会为了异常巨型名称扫描完整输入。
- 应用自己的日期备份名保持原样，对象 URL 的延迟撤销和失败清理契约不变。

### 验证

- 覆盖路径分隔符、控制字符、尾点、`CON.json`、空值及 100 个 emoji 后接禁止尾标的名称；结果便携、有界、无孤立代理项且不会读取预算后的尾部。

## 0.173.0 · 2026-08-28

### 复制载荷预算

- 纯文本复制入口新增 64 KiB 核心上限，异常调用不能再把任意大字符串直接交给 Clipboard API 或隐藏文本域。
- 超限判断发生在读取浏览器剪贴板与 DOM 能力之前；正常复航简报和工作区交接清单远低于该预算，现有复制路径保持不变。
- 边界值仍可复制，空内容、异步剪贴板优先、权限拒绝后的 DOM 后备与焦点恢复继续遵守原契约。

### 验证

- 传入 65,537 字符并把 `clipboard`、`document` 依赖设为读取即抛错 getter，复制稳定返回长度错误且 getter 零读取；恰好 65,536 字符仍由 Clipboard API 接收。

## 0.172.0 · 2026-08-28

### 有界交接输入槽

- 工作区交接清单除 5 个项目和 4 条关注项的输出上限外，新增每类最多读取 20 个输入槽的独立预算。
- 活动会话定位、优先复航项目筛选和关注项筛选都按索引在该窗口内运行；畸形数组即使以大量空项开头，也不会为寻找有效项遍历全部 50,000 槽。
- 正常界面本来只向构建器传入 5 个已排名项目，因而当前会话、项目与关注内容保持不变；较宽 20 槽只为公开 API 容纳少量无效元素。

### 验证

- 在排名和关注数组第 21 项设置读取即抛错 getter、把长度扩到 50,000，并把唯一有效项放在第 20 项；清单仍呈现会话、项目与原因且不越界读取。

## 0.171.0 · 2026-08-28

### 流式简报证据窗

- 复航简报不再先对任意长度的未决信号和准备缺口执行 `map/filter/join`，而按卡片生产契约只读取前三条信号和前六条缺口。
- 每项先服从模型字段长度，拼接过程每一步再次压到最终 800/1,200 字符预算，避免中间字符串或投影数组随输入长度增长。
- 正常卡片本来最多产生三条未决信号和有限缺口，因此现有简报信息不丢失；独立 API 的畸形数组也不会读取窗口之外的 getter。

### 验证

- 构造长度 50,000 且窗口下一项读取即抛错的信号/缺口数组，简报仍完整包含允许窗口、总长小于 3,000 字符且不触发越界读取。

## 0.170.0 · 2026-08-28

### 资源文本扫描窗

- HTTP(S) 资源提取新增 4,096 字符证据扫描上限，在构造全局正则迭代器前拒绝畸形超长输入。
- 正常项目说明和检查点四字段的严格持久化上限合计低于该窗口，现有资源识别范围不缩减；单 URL 2,048 字符、展示标签 180 单位和最多 20 条仍独立生效。
- 直接调用核心函数不再依赖业务模型或 HTML 输入限长来避免对巨型字符串做完整扫描。

### 验证

- 模拟 `String.prototype.matchAll` 一旦调用就抛错，再传入超过共享窗口的文本；函数仍直接返回空数组，证明拒绝先于正则扫描。

## 0.169.0 · 2026-08-28

### 核心筛选查询预算

- 快速记录的项目筛选将 100 字符预算从 HTML 提示提升为核心契约，原始查询或 NFKC 规范化结果任一超限都会立即拒绝。
- 超限路径在访问项目集合前返回冻结空窗口并标记 `queryRejected`，不会对最多 50,000 个项目读取标题、说明和下一步。
- 界面 `maxlength` 复用同一共享常量，并能为程序化畸形值显示明确长度提示；正常精确、前缀、标题和上下文排名保持不变。

### 验证

- 新增带抛错 `projects` getter 的超限查询测试，证明拒绝发生于集合读取之前，并核对空窗口、拒绝标记与共享上限。

## 0.168.0 · 2026-08-28

### 可降级唯一标识

- 记录 ID 生成不再假定已暴露的 `crypto.randomUUID()` 一定可调用；安全提供者抛错或返回空值时会进入受控后备路径。
- 后备标识组合当前时间、页面内单调序号与尽力随机片段；即使同一毫秒且随机源固定或拒绝，单页连续创建仍不会碰撞。
- 跨标签写入租约复用同一生成器，不再维护第二套直接调用加密 API 的脆弱逻辑；正常浏览器仍优先使用原生 UUID。

### 验证

- 模拟 `randomUUID` 抛错、固定系统时间与固定伪随机值，连续生成的带前缀 ID 仍格式稳定且彼此不同；存储租约既有并发测试继续通过。

## 0.167.0 · 2026-08-28

### 无歧义绝对时间

- 日期格式化、相对时间、用时和离开天数不再允许 `Date` 隐式把 `null`、布尔值、数组、普通对象或空白字符串解释为 1970 年时间。
- 时间输入现只接受有限数字、有效 `Date` 或非空日期字符串；其他值稳定降级为“时间未知”或零，不制造虚假的历史活动。
- 超过相对时间窗口的日期与所有绝对日期新增年份，跨年检查点、归档和备份来源不再只显示月日而产生歧义。

### 验证

- 扩充时间测试覆盖七类强制转换输入，并用带年份的同配置格式器核对日期与 30 天切换边界。

## 0.166.0 · 2026-08-28

### 紧凑资源可访问名称

- 复航资源继续保留最多 2,048 字符的完整、安全 HTTP(S) 目标，但展示标签统一限制为 180 个 UTF-16 单位。
- 标签从主机开始并按 Unicode 安全边界省略冗长路径，避免单条合法链接在可见列表和 `aria-label` 中重复放大；协议、查询和完整路径仍只存在于实际 `href`。
- URL 凭据、控制字符、混合主机标签、非 HTTP 协议与畸形编码的既有拒绝规则保持不变。

### 验证

- 新增长路径资源测试，确认完整目标未被改写、标签带省略号且不超过共享 180 单位契约。

## 0.165.0 · 2026-08-28

### 有界导入来源标签

- 导入预览不再把文件对象的原始 `name` 直接保留到临时状态和 DOM，而是先规范首尾空白并限制为 160 个 UTF-16 单位。
- 文件名边界复用 Unicode 安全紧凑函数，截断不会留下孤立代理字符；空白、缺失或非字符串名称统一显示“未命名备份.json”。
- 该限窗只影响来源标签，不改变文件读取、25 MB 字节上限、内容校验或导入结果。

### 验证

- 新增文件标签纯函数测试，覆盖正常名称、空白回退、超长表情符号边界和最终长度。

## 0.164.0 · 2026-08-28

### 稳定异常说明

- 界面提交、生命周期动作、撤销、复制和导入的异常提示统一通过同一个安全描述函数，不再直接假定捕获值一定具有非空 `message`。
- 浏览器 API 以字符串、空对象或空消息拒绝时，界面改为明确提示根据最新状态重试；存储层则统一说明访问被拒绝，不再向通知或保存错误投影 `undefined`。
- 正常 `Error` 与 DOM 异常的具体消息仍原样保留，随后继续服从通知的 500 字符输出边界与 HTML 转义。

### 验证

- 新增纯函数测试覆盖标准错误、空消息和原始值拒绝，并让存储主写入直接抛出字符串，确认状态不提交且错误稳定降级。

## 0.163.0 · 2026-08-28

### 自承载启动降级

- 启动异常界面提炼为无依赖、可单测的安全渲染器；正常 `#app` 存在时继续清除忙碌态并提供显式重试。
- 若入口挂载节点意外缺失，降级路径会在可用的 `body` 中补建同名节点，不再因对 `null` 写属性而遮蔽原始启动异常。
- DOM 尚不可用、挂载对象畸形或安全策略阻止写入时，错误处理返回安静失败，避免异常处理本身继续抛错；故障详情仍只进入控制台，不插入 HTML。

### 验证

- 新增启动降级单测，覆盖已有挂载、缺失挂载自动补建、重试绑定，以及无 DOM、畸形挂载和写入抛错三类安全退出。
- 离线外壳、HTTP 白名单与语法检查同步纳入新核心模块，并继续由发布契约核对运行时模块闭包。

## 0.162.0 · 2026-08-28

### 规范备份导出时间

- 备份信封的可选 `exportedAt` 元数据从宽松 `Date.parse` 收紧为应用统一的毫秒 UTC ISO 往返校验。
- 无毫秒 UTC、带时区偏移、地区日期格式和首尾空白不再被当作可信导出时间展示，避免同一备份在不同浏览器产生歧义标签。
- 该字段只影响来源说明；有效数据本体、旧信封兼容、校验码验证与最终逻辑时间均保持原语义。

### 验证

- 导入预览回归分别传入四类宽松可解析时间并确认来源时间为空；规范 `2026-08-27T12:00:00.000Z` 仍原样保留。

## 0.161.0 · 2026-08-28

### 双层诊断尾窗

- 存储层“最近八条”限制提升为共享契约，界面启动接收与每轮重绘接收都使用同一 `STORE_NOTICE_LIMIT`，不再复制魔法数字。
- 诊断在渲染失败后仍可保留，但反复失败与新异常交错时只留下最新八条；最旧未展示消息被有界淘汰，近期故障上下文优先。
- 成功提交后消费、单条 500 字符上限与 HTML 转义保持不变，正常路径仍只展示一次。

### 验证

- 存储测试直接以共享常量断言队列长度；界面契约确认导入同一常量，并在接收后按超出数量移除队首，通知投影本身仍保持只读。

## 0.160.0 · 2026-08-28

### 原子消费恢复诊断

- 存储恢复、跨标签与容量诊断不再由 HTML 生成函数提前 `splice`；渲染先只读投影队列，根应用壳成功提交后才一次性清空。
- 若路由投影、对话框生成或根 DOM 写入在诊断展示前失败，消息仍留在内存，下一次安全重绘可以继续说明发生过的恢复行为。
- 已成功提交的消息仍只显示一次，不改变存储层八条限窗、HTML 转义或无障碍页面结构。

### 验证

- 界面契约确认通知投影只调用 `map` 且不含移除操作，并以源码顺序证明 `root.innerHTML` 提交发生在队列清空之前。

## 0.159.0 · 2026-08-28

### 统一内联草稿边界

- 活动会话页的内联轨迹草稿恢复不再直接调用 UTF-16 `slice`，而与普通对话框共用控件长度和 Unicode 安全截断函数。
- 外部标签刷新期间即使程序化草稿超过 1200 字符且边界落在 emoji 中间，恢复值也保持完整字符并严格不超过持久化模型上限。
- 会话 ID、记录类型、焦点和选区的既有绑定保持；选区继续按最终安全文本长度收拢。

### 验证

- 界面契约明确要求内联恢复调用共享边界函数并传入 `IMPORT_LIMITS.crumbText`，同时保留同会话、类型白名单和选区恢复检查。

## 0.158.0 · 2026-08-28

### Unicode 安全的草稿边界

- 对话框快照在达到控件或全局长度上限时检查截断点；若边界正好切开 UTF-16 代理对，会回退一个码元而不是生成孤立半个 emoji。
- 只有“截断产生的完整代理对断裂”会触发回退；输入原本包含的孤立码元不会被悄悄改写，边界函数仍保持可预测和无损于限窗内原文。
- 选区继续相对最终安全文本夹紧，因此回退后的光标不会越过实际值长度。

### 验证

- 使用 `a🚀b` 验证 2 单位边界回退为 `a`、3 单位边界完整保留 `a🚀`；另以原生孤立高代理确认不会误判。

## 0.157.0 · 2026-08-28

### 有界对话框快照

- 外部重绘保存普通对话框草稿时优先服从控件 `maxlength`，无声明或声明过大时仍以 2400 字符为全局上限，程序化异常值不会扩大临时内存。
- 恢复前再次按新控件边界截断，选择框只采用仍存在的选项；复选与单选仍只恢复布尔状态，不把文本值写入控件。
- 文本选区要求安全整数且非负，并按截断后的实际长度夹紧、校正首尾顺序；非法位置不调用 `setSelectionRange`。

### 验证

- 纯函数回归覆盖控件上限、全局上限、空值与数值转换，以及正常、反向、超界、负数、小数和空选区；界面契约回归保持实体绑定与焦点恢复。

## 0.156.0 · 2026-08-28

### 实体绑定的对话框恢复

- 外部标签页重绘前的对话框快照新增上下文键：开始会话绑定项目，检查点绑定活动会话，快速复核绑定项目与源检查点，编辑和归档绑定目标项目。
- 恢复前比较新 DOM 的实际上下文；项目、会话或检查点切换后旧草稿不再灌入同名新对话框，失效时给出可见提示并清理相应待处理令牌。
- 项目编辑恢复会重新核对字段级编辑令牌，归档恢复会重新核对生命周期与活动会话约束；开始会话恢复还拒绝外部新出现的活动现场。

### 验证

- 界面契约确认快照记录上下文键、恢复入口先过验证门、三种数据对话框使用实际隐藏字段或 `data-context-id`，编辑/归档/开始会话复用核心计划校验。

## 0.155.0 · 2026-08-28

### 工作区绑定的分页记忆

- 轨迹和项目分页记忆现在绑定工作区创建标识；导入、重置或跨标签切换到另一工作区时立即清空，不把旧工作区的展开深度带入新数据。
- 单个工作区最多记住最近展开的 24 个项目轨迹窗口；再次展开会刷新顺序，超过边界淘汰最早记录，长期浏览大量项目也保持常量级界面状态。
- 应用销毁显式释放分页 Map、工作区标识与搜索索引引用，减少单页宿主卸载后的对象保留面。

### 验证

- 界面契约确认工作区标识变化同时清空两类分页记忆，轨迹记忆先刷新键顺序再执行 24 项淘汰，并在销毁路径释放缓存与索引。

## 0.154.0 · 2026-08-28

### 会话绑定的过期确认

- “继续原会话”的确认由永久增长的会话 ID 集合收敛为单一令牌；它只能绑定当前唯一活动会话，符合系统不允许多个活动现场的约束。
- 渲染发现现场结束、接续、切换、导入或跨标签替换时自动清除确认；销毁也显式释放，不让历史会话在长期开启的页面中累积。
- 旧 DOM 的迟到点击先核对当前活动会话 ID，不再能误确认已变化的现场；计时刷新与警告渲染共享同一绑定判断。

### 验证

- 界面契约确认不再存在确认集合，渲染会清除不匹配令牌，确认动作要求当前会话 ID 一致，警告与计时签名都只接受当前绑定。

## 0.153.0 · 2026-08-28

### 有界存储诊断队列

- 存储层的启动恢复、跨标签、配额、写后复核和订阅异常统一通过诊断入口，单条压到 500 字符，只保留最近八条。
- 诊断在尚未挂载界面或短时间反复读写失败时也不会无限增长；淘汰最旧信息以保留最新故障，排空语义不变。
- 已知简短诊断原文保持不变，界面每次渲染仍原子排空并转义，公共状态与备份格式不新增字段。

### 验证

- 构造首次读取成功、之后连续二十次抛出千字符错误的存储源，队列最终严格保留八条且每条不超过 500 字符；首次排空返回全部窗口，第二次排空为空。

## 0.152.0 · 2026-08-28

### 有界通知窗口

- 可见 toast 固定保留最近四条，单条消息压到 500 字符；高频错误或异常长底层消息不会无限扩大 DOM 与无障碍播报队列。
- 新通知超过窗口时立即移除最旧节点、取消并删除其计时器；重绘路径再次截取最近窗口，过期通知仍按时间过滤。
- 应用销毁后新增和延迟移除通知都短路，既不会重新建立计时器，也不会修改已释放界面。

### 验证

- 界面契约确认消息压缩、四条循环淘汰、计时器清理、重绘尾部窗口和销毁保护全部存在；聚焦回归保持异步请求门与通知渲染语义。

## 0.151.0 · 2026-08-28

### 渲染序号隔离延迟回调

- 每次渲染分配单调序号；下一帧的导入对话框重开、普通对话框恢复和延迟焦点只有在仍属于最新渲染时才执行，连续外部写入不会让旧快照落到新 DOM。
- 应用销毁改为幂等并使当前渲染序号失效；销毁后的用户动作、命令下一帧、对话框聚焦和无障碍播报全部短路。
- 对话框和播报回调还要求目标节点仍连接到文档；被重绘替换的旧节点不会接收焦点或文字。

### 验证

- 界面契约确认三类渲染延迟回调同时比较销毁标记和渲染序号；命令回调检查销毁，聚焦与播报检查节点连接；重复销毁在释放资源前直接返回。

## 0.150.0 · 2026-08-28

### 外部刷新保留同会话轨迹草稿

- 外部标签页写入触发重绘前，若当前活动现场的内联轨迹输入有文字或焦点，暂存会话 ID、文字、类型、焦点和选区；普通本地重绘不额外保留陈旧草稿。
- 新页面只在唯一活动会话 ID 仍与快照一致时恢复草稿；会话已停靠、接续、切换、冲突或项目页不再显示输入框时直接丢弃，避免把旧文字搬到新现场。
- 恢复文字再次截断到持久化长度边界，类型必须仍是已知记录类型；对话框打开时不捕获后台表单，焦点和选区只在原控件仍适用时恢复。

### 验证

- 界面契约确认外部重绘前捕获、重绘后恢复；捕获绑定当前会话并跳过空闲空表单，恢复先比较会话 ID，再执行文字边界、类型白名单和选区恢复。

## 0.149.0 · 2026-08-28

### 检查点表单绑定活动会话

- 打开可靠检查点对话框时先定位唯一活动现场，并在内存中保存当时的会话 ID；没有活动会话或会话冲突时不再打开表单。
- 提交时重新定位当前唯一现场，要求会话 ID 与打开时完全一致，再沿用稳定会话/项目位置执行事务复核；旧会话已停靠、被接续或切换后，旧表单不会串写到新会话。
- 取消、关闭、成功提交和应用销毁都会清除绑定；外部状态刷新仍保留用户输入与原绑定，使陈旧状态可被明确检测而不是静默改绑。

### 验证

- 界面契约确认打开动作保存活动会话 ID，提交动作先检查绑定存在并比较重新定位的 ID，随后才构建与保存检查点；所有关闭路径清除绑定。

## 0.148.0 · 2026-08-28

### 多标签页编辑令牌

- 打开项目编辑框时保存只覆盖可编辑字段的紧凑令牌；外部标签页刷新页面投影时保留令牌和用户尚未提交的输入，不把新版隐藏基线伪装成原始表单。
- 提交事务重新定位未归档项目，并逐字段核对标题、目的、下一步及对应更新时间；任一字段变化都会拒绝陈旧表单并要求重新核对。
- 令牌比较不只依赖 `updatedAt`，因此同一毫秒内发生的外部字段变化也不会被旧表单覆盖；无关项目变化不会使当前编辑失效。

### 验证

- 使用完全相同的项目更新时间但改变标题，核心编辑计划仍拒绝旧令牌；匹配令牌可重复验证。界面契约确认打开时保存令牌、提交时使用令牌，并校验表单项目身份。

## 0.147.0 · 2026-08-28

### 项目生命周期变更守卫

- 编辑、普通状态切换和从归档恢复分别使用核心准备计划；每个计划单遍定位目标并返回稳定位置，同时验证该操作要求的归档状态。
- 归档项目无法通过仍打开的陈旧编辑表单被修改，也不能借旧状态控件直接变回活动/暂泊/受阻；恢复动作只接受仍在归档舱的项目。
- 合法的未归档编辑和状态切换、合法的归档恢复保持原有时间单调更新语义，界面事务不再自行 `find()` 项目。

### 验证

- 禁用项目数组 `find()` 后，三个准备计划准确返回前后位目标；只读编辑、归档状态跳转、重复恢复、缺失项目和非法目标状态均按意图拒绝。界面契约确认三个事务只调用对应计划。

## 0.146.0 · 2026-08-28

### 交互路径复用唯一活动现场

- 主渲染把工作区帧解析出的唯一活动会话保存为短生命周期上下文；多活动会话时该值严格为空，不会退回任意首项。
- 每秒计时健康检查直接复用该上下文，不再每秒遍历全部会话；跨项目快捷记录的筛选输入也不再为每个字符重新扫描并选择活动会话。
- Ctrl/⌘ Shift C 直接读取同一渲染计数判断是否存在可记录项目，不再额外遍历项目集合；状态发布仍会同步重绘并原子替换两个上下文值。

### 验证

- 界面契约确认主渲染发布唯一会话；计时刷新和项目筛选只读该值且没有 `sessions.find()`，快捷键只读未归档项目计数且没有 `projects.some()`。

## 0.145.0 · 2026-08-28

### 原子归档准备计划

- 新增项目归档准备计划，单遍定位项目及稳定位置、单遍检查其活动会话，并用清楚错误拒绝缺失、已归档或仍在工作的目标。
- 打开确认框和最终提交共享同一核心规则；提交在存储事务的克隆快照内重新准备计划，再按稳定位置复核身份与状态后修改，确认期间出现的新活动会话不会被归档。
- 对话重绘直接复用渲染帧已经解析的当前路由项目显示标题，不再为待归档 ID 额外扫描项目集合。

### 验证

- 禁用项目和会话数组的 `find()` / `some()` 后，后位目标仍返回准确位置；活动会话、缺失目标和已归档目标均拒绝。界面契约确认准备、重绘和提交路径不再自行使用这些数组助手。

## 0.144.0 · 2026-08-28

### 单遍工作区渲染帧

- 新增工作区帧投影：先单遍会话确定活动数量与唯一候选，再单遍项目同时统计状态、定位当前路由项目和唯一活动项目；今日轨迹计数继续共享同一个时间快照。
- 主渲染直接解构帧，不再在计数之外分别 `find()` 当前项目、活动会话和所属项目；命令可用性、冲突警报、页头与悬浮停靠来自同一快照。
- 多活动会话帧保留真实计数但把活动会话和项目都置空，防御性状态不会泄漏首项选择；既有 `buildWorkspaceCounts` 保持兼容并只投影帧中的计数。

### 验证

- 禁用项目与会话数组的 `find()` 后，后位路由项目、唯一活动会话及所属项目准确返回；加入第二个活动会话后计数为二且上下文都为空。界面契约确认主渲染不再自行查找项目或会话。

## 0.143.0 · 2026-08-28

### 渲染阶段不再任意选择冲突会话

- 页面先生成工作区计数，仅在活动会话数恰好为一时才定位并展示该会话；零个或多个活动会话不会再由 `find()` 任意挑出首项。
- 防御性遇到多个活动会话时，主内容顶部显示带数量的可访问警报并停止选择现场；唯一会话的项目引用缺失时同样隐藏悬浮会话并提示从数据保险箱恢复有效备份。
- 正常有效工作区的页头、悬浮停靠、计时与项目上下文保持不变，命令面板和主渲染现在消费同一活动会话计数语义。

### 验证

- 界面契约确认计数发生在会话定位之前，只有 `activeSessions === 1` 才调用 `find()`；冲突数和缺失项目两类警报均为 `role="alert"`，并且主内容渲染始终包含诊断入口。

## 0.142.0 · 2026-08-28

### 快捷命令复用工作区计数

- 工作区计数在既有流式遍历中新增活动会话数量；快捷命令面板直接复用当前渲染快照，不再为“有项目”和“有活动会话”分别扫描项目与会话数组。
- 唯一活动会话继续显示应急停靠；若防御性地遇到多个活动会话，命令改为禁用的“会话冲突”并说明应恢复有效备份，不把含糊停靠呈现为普通动作。
- 空搜索输入与首次对话渲染共享同一计数对象，状态发布后的下一次渲染原子替换快照，不额外持久化或缓存集合副本。

### 验证

- 混合三种项目状态、归档项目和两个活动会话时，流式计数准确；界面契约确认命令面板不调用项目/会话 `some()`，并以活动会话计数决定正常或禁用冲突动作。

## 0.141.0 · 2026-08-28

### 会话启动对话的单遍准备计划

- 打开“开始会话”对话框时，核心计划一次扫描会话集合验证唯一活动会话，再一次扫描项目集合同时定位目标项目、稳定位置与活动项目。
- 界面不再分别查找活动会话、阻塞项目和目标项目；提示直接使用计划返回的活动项目，目标不可用、活动引用损坏或多个活动会话均给出明确错误。
- 真正提交仍重新运行启动计划并额外单遍选择最新来源检查点，保留对对话打开后外部标签页变化的陈旧操作保护。

### 验证

- 禁用项目/会话数组的 `find()` 后，计划准确返回后位目标、索引、唯一活动会话及其项目；多活动会话和缺失活动项目均拒绝。界面契约确认对话准备不再自行扫描集合。

## 0.140.0 · 2026-08-28

### 语义相同外部写入的幂等同步

- 外部标签页原文变化后仍先执行完整结构、引用、修订与时间验证；若规范化状态与当前规范原文完全相同，只更新实际主键原文基线，不替换冻结状态、不发通知也不触发界面重绘。
- 不同缩进、空白或等价字段排列不再被误报成“相同修订号的不同内容”；真正同修订但语义不同的碰撞仍采用实际持久化胜者并告警。
- 幂等同步仍刷新写入基线，因此下一次本地编辑可从等价格式化原文安全覆盖到后继修订，而不会被陈旧标签防护误拦。

### 验证

- 把当前状态以两空格缩进重新写入主键后刷新：返回未变化、状态对象引用不变、无通知和订阅事件；随后本地编辑成功写入下一修订。既有真实同修订碰撞、倒退与损坏隔离测试保持通过。

## 0.139.0 · 2026-08-28

### 对象备份复用规范原文

- 程序化对象备份不再先 `structuredClone` 整个工作区、再为校验重复序列化；现在从已提交规范原文解析出分离副本，并直接扫描同一原文计算校验。
- 对象备份仍保持完全可变且与冻结状态深分离，信封字段和导入语义不变；导出时间新增可注入参数，测试与调用者可在时钟回拨场景获得确定结果。
- 文本版与对象版备份现在共享同一份状态字节语义，避免两个出口因不同克隆/序列化路径产生隐性偏差。

### 验证

- 禁用 `structuredClone` 后对象备份仍成功，时间准确、数据深相等且非同一引用；修改导出副本不影响当前状态，校验和完整往返测试继续通过。

## 0.138.0 · 2026-08-28

### 有界且快照化的来源占用检查

- 来源存储占用检查只读取一次条目数量并固定扫描窗口，避免异常存储在循环期间持续增长 `length` 导致设置页无界遍历。
- 最多检查 10,000 个来源键；长度 getter 拒绝访问、条目数不可信、值不是字符串或 UTF-16 字节累计失去安全整数精度时统一回退为“不可用”，不展示伪造的正常占用。
- 键和值仍按浏览器 `localStorage` 的 UTF-16 保守模型计量，应用键与整个来源总量的既有告警阈值保持不变。

### 验证

- 使用第二次读取即膨胀到最大安全整数的动态 `length`，确认检查器只读取一次并准确计量首个键；抛错 getter、10,001 项和非字符串值均安全回退，既有临界/预警阈值测试保持通过。

## 0.137.0 · 2026-08-28

### 复用已提交状态的规范原文

- 存储层现在把规范状态 JSON 与冻结状态作为同一发布快照维护：启动恢复时初始化，本地成功提交时复用已经写入的原文，外部标签页采用时从严格验证后的状态重建。
- 紧凑备份文本直接使用该内存基线；反复进入设置页估算容量或随后导出时，不再重复遍历和序列化整个工作区，只重新扫描原文计算校验并生成小型元数据头。
- 缓存不写入新的键、不扩大磁盘占用，也不绕过主键最终确认；只有状态真正发布时才切换，保存失败或外部值被隔离时仍保持旧基线。

### 验证

- 成功提交后拦截并禁止对当前冻结状态再次调用 `JSON.stringify`，连续两次文本导出仍完全相同、可解析且数据准确；既有外部同步、碰撞、恢复与全套备份往返继续通过。

## 0.136.0 · 2026-08-28

### 单份数据序列化的紧凑备份

- 新增备份文本出口：工作区只做一次规范 JSON 序列化，FNV-1a 校验直接消费该文本，再把小型信封元数据与数据片段组合成完整备份。
- 设置页容量估算与实际下载都改用紧凑文本，不再为一次导出依次持有整份结构化克隆、校验 JSON 与缩进 JSON；对象版导出仍保留给程序化调用和旧测试使用。
- 备份信封字段、校验算法与导入兼容性不变，仅移除对机器恢复无意义的缩进空白，接近浏览器存储参考线时峰值内存和下载体积更低。

### 验证

- 禁用 `structuredClone` 后文本导出仍成功，解析结果与冻结状态相等并通过真实导入预览校验；规范文本无换行且小于缩进对象导出。界面契约确认容量估算和下载不再调用对象导出或二次 `JSON.stringify`。

## 0.135.0 · 2026-08-28

### 交接原因的严格有界组装

- 工作区交接清单对每个关注项最多读取三个原因，并在每次追加时按检查点摘要边界压平，不再先遍历任意长度原因数组再做最终截断。
- 畸形排名项或关注项会被跳过而不是提前终止整个窗口；活动会话摘要也要求项目上下文完整，公共纯函数面对部分损坏输入时仍能保留后续有效内容。
- 五项目、四关注项、三原因与字段长度共同构成明确的输出上界，复制操作的内存规模不再受调用者提供的原因集合长度影响。

### 验证

- 用长度伪装为五万且第四项读取即抛错的原因数组验证只访问前三项；在排名和关注窗口前插入畸形项，后续有效项目仍被输出，整份清单保持小于固定千字符量级。

## 0.134.0 · 2026-08-28

### 当前会话停靠的唯一解析

- 键盘应急停靠与命令面板不再各自扫描并预选第一个活动会话，而是把“当前会话”直接交给核心停靠计划解析。
- 核心计划一次性验证零个、唯一或冲突活动会话并返回稳定位置；同步事务只信任计划返回的真实会话 ID，损坏状态不会经过含糊的预选路径。
- 项目内显式停靠仍携带目标会话 ID，继续保留“界面所见会话必须仍是唯一活动会话”的陈旧操作保护。

### 验证

- 界面契约确认键盘与命令入口不再扫描会话数组、均以未指定 ID 调用核心计划；停靠事务使用计划解析的 ID 与位置，既有显式目标、无会话与多会话拒绝测试保持通过。

## 0.133.0 · 2026-08-28

### 有界的工作区交接清单

- 首页新增“复制交接清单”，把当前会话、最多五个优先复航项目、最多四个关注项和七日航迹压成便于发给未来自己或协作者的纯文本。
- 每个项目只保留状态、复航完整度与第一动作；字段沿用持久化长度边界，项目/关注数量固定封顶，大工作区不会生成集合等长字符串。
- 交接清单明确标注只根据本机证据生成、不评价产出；复制复用异步请求门，较旧剪贴板完成不会覆盖最新反馈。

### 验证

- 用八个排名项目、活动会话、关注原因和周度指标生成清单；输出仅含前五项、状态/百分比/下一步准确，且空概览与异常可选指标安全回退。界面契约确认按钮有可访问名称并使用五项排名上限。

## 0.132.0 · 2026-08-28

### 会话启动准备计划

- 会话启动核心准备一次确认全局没有活动会话、定位非归档目标项目，并选择该项目最新来源检查点；返回项目对象、稳定位置和来源检查点。
- 界面按计划创建会话，事务通过项目位置复核身份与状态后再追加会话并更新时间，不再在原状态和克隆状态分别查找项目。
- 全局单活动会话、归档拒绝、来源检查点 ID、时间回拨钳制与暂停项目自动恢复推进状态保持。

### 验证

- 禁用项目、会话和检查点数组的 `find()`，把目标与最新检查点放在后位；计划返回准确项目位置与来源，并拒绝活动会话、缺失项目和归档项目。界面契约确认事务按位置复核且不构建完整复航卡。

## 0.131.0 · 2026-08-28

### 同修订碰撞后的自动解锁

- 不认识租约的旧标签可能在当前页发布后写入相同修订号的不同有效内容；过去该原文会被当作“未前进”永久拒绝，使当前页后续每次保存都撞上同一差异。
- 外部内容通过严格结构/引用验证、修订号相同且工作区时间不倒退时，现在采用实际持久化版本并给出冲突通知；下一次本地保存自然递增到下一修订。
- 真正较低的修订号仍作为回滚拒绝，同修订但时间倒退也继续失败关闭；损坏或引用不一致的外部原文仍隔离。

### 验证

- 注入同修订号、不同项目且时间前进的外部状态；刷新采用实际存储胜者并以 `external` 来源通知，随后的本地编辑成功保存为下一修订。低修订回滚与时间回滚测试保持拒绝。

## 0.130.0 · 2026-08-28

### 内存发布前的最终主键确认

- 覆盖已有数据时，主键首次写后确认与滚动快照尽力保存之间仍可能被不认识租约的旧标签插入；现在快照操作完成后再次确认主键实际原文。
- 只有最终确认仍为本地序列化结果，才更新持久化原文基线并发布候选到内存；若外部前进版本已到达，则采用它并拒绝本地成功反馈。
- 首次保存和已释放快照的配额重试没有辅助写入间隙，继续使用紧邻主写入的确认；只读复核受限仍沿用诊断通知而不误报标准同步写入失败。

### 验证

- 在滚动快照 `setItem` 返回时注入外部主数据替换；最终确认识别外部胜者，当前页不发布本地候选并可靠释放租约。既有提交前、主写后和配额交错测试继续通过。

## 0.129.0 · 2026-08-28

### 原子的快速停靠计划

- 快速停靠核心现在一次产出低置信度检查点输入、唯一活动会话、所属项目及两者稳定位置；原有输入派生 API 委托该计划并保持返回结构兼容。
- 界面不再在核心派生前后重复查找会话与项目；提交事务按计划位置复核会话仍活动、项目身份与非归档状态，再写入检查点或接续会话。
- 当前时间/陈旧阈值参数验证、指定会话一致性、证据优先级、有界未决事项与占位回退保持。

### 验证

- 禁用项目与会话数组的 `find()`，把活动现场置于后位；计划返回准确双索引，其输入与兼容 API 完全一致。界面契约确认停靠准备和事务均不再线性查找项目/会话。

## 0.128.0 · 2026-08-28

### 活动现场的稳定事务定位

- 新增单遍活动现场定位器，一次返回唯一活动会话、所属项目及两者数组位置；多活动会话、悬空项目或归档项目均明确拒绝。
- 普通轨迹记录与手工检查点保存复用这些位置，在事务克隆中先复核会话仍活动、项目身份与状态仍有效，再执行任何追加或字段更新。
- 两条高频路径不再在准备和事务阶段分别扫描会话/项目集合，时间钳制、下一步投影、会话关闭与提示语义保持。

### 验证

- 禁用项目与会话数组的 `find()` 并把活动现场放在后位；定位器返回准确对象与位置，并拒绝冲突、悬空和归档现场。界面契约确认两种事务只按稳定位置复核，不再重复查找。

## 0.127.0 · 2026-08-28

### 快速检查点复核的稳定项目槽位

- 快速检查点升级准备在验证项目与最新待复核记录时同步保存项目数组位置，不再调用通用 ID 查找。
- 提交事务直接复用该位置，并在追加可靠检查点前复核项目 ID 与非归档状态；不符合时克隆事务整体放弃。
- 陈旧来源检查点、占位摘要/下一步、字段长度、时间钳制和可靠检查点创建语义保持不变。

### 验证

- 禁用项目数组的 `find()` 并把目标放在后位；复核准备仍返回准确项目位置与标题。界面契约确认事务按位置复核，不再二次扫描项目集合。

## 0.126.0 · 2026-08-28

### 快速记录的稳定项目槽位

- 快速记录准备阶段以单遍循环定位目标项目和其活动会话，并返回经验证的项目数组位置；不再依赖源集合的 `find()`。
- 持久化事务克隆保持项目顺序，因而可直接复用该位置；写入轨迹前仍复核项目 ID 与归档状态，异常时整笔事务回滚。
- 目标标题、活动会话关联、事件时间钳制和下一步投影语义保持不变，提交后提示仍来自同一份准备结果。

### 验证

- 禁用项目与会话数组的 `find()`，把目标与活动会话放在后位；准备函数返回准确位置与会话关联。界面契约确认事务直接索引并复核槽位，不再第二次线性查找。

## 0.125.0 · 2026-08-28

### 轻量的会话来源检查点

- 开始会话只需记录来源检查点 ID；现在通过单遍检查点选择器取得目标项目的最新项，不再为此构建完整复航卡。
- 选择器不扫描项目、会话或轨迹，不建立索引、排序数组或完整度投影；同时间检查点继续以较晚写入者为新，保持既有确定性语义。
- 项目存在性、归档状态和全局单活动会话约束仍在会话创建前独立验证。

### 验证

- 混合无关项目、旧检查点与同毫秒检查点，并禁用源数组的 `filter()`、`find()`、`map()`、`sort()`；选择器准确返回目标项目最后写入的最新项。界面契约确认会话开始路径不再调用完整复航卡构建。

## 0.124.0 · 2026-08-28

### 单遍轨迹动作反馈

- 解决/重新打开问题与阻塞、置顶/取消置顶航标时，最终布尔状态直接从同一次持久化事务配方带出。
- 提交成功后的播报与提示不再重新扫描整个轨迹集合；在 50,000 条记录边界上，每次动作减少一次线性查找。
- 保存失败仍由共享动作边界接管，事务内的临时结果不会产生成功播报。

### 验证

- 源码契约分别限定两个动作处理器只执行一次轨迹查找，并确认反馈状态来自已提交配方而非提交后的 `getState()` 再扫描；完整界面与存储门禁继续通过。

## 0.123.0 · 2026-08-28

### 主写入结果确认

- 每次主键 `setItem` 返回后立即读取实际原文；不认识租约的旧标签若恰在写入返回后替换数据，当前页会严格采用或隔离外部结果，并拒绝发布已被取代的本地候选。
- 标准存储写入已经返回成功、但紧随其后的只读复核被浏览器拒绝时，不误报写入失败；程序保留提交并留下可见诊断通知。
- 配额回收重试被外部写入取代时，仅在滚动快照键仍为空的前提下恢复本地旧副本，绝不覆盖外部标签刚建立的恢复点。

### 验证

- 覆盖普通写入后被替换、成功写入后复核读取被拒，以及配额重试后外部主数据与外部快照同时到达三种交错；内存、主键、快照和租约均保持相符。

## 0.122.0 · 2026-08-28

### 写入前的主数据复核

- 标签页取得短租约并完成序列化后，在每一次主键写入前同时复核租约与主数据原文；不认识租约的旧版本标签页若在序列化期间写入，新版本会采用或隔离其结果并拒绝覆盖。
- 首次主写入遇到配额不足时，释放滚动撤销快照之前再次执行相同复核；外部提交已经到达时不会为了当前重试删除可恢复副本。
- 既有修订号、工作区时间和持久化原文三层保护保持，短租约仍在成功、冲突与异常路径中可靠释放。

### 验证

- 注入两个确定性交错：旧标签在序列化后写入，以及首次主写入报配额后、快照回收前写入。两者都保留外部胜者，拒绝本地覆盖并释放租约；配额路径还保持原滚动快照。

## 0.121.0 · 2026-08-28

### 有界的首页项目排名

- 工作区概览先为全部未归档项目建立一次共享复航卡集合，周报和关注清单继续消费完整集合；首页推荐只用最差项在根的有界堆保留当前可见 K 项。
- 首屏 K 为 12，“继续显示”每次增加 12 并重新计算对应窗口；总项目数来自完整卡片计数，分页剩余量不因有界排名而缩短。
- 推荐分数、最近活动与代码单元 ID 的确定性全序保持；需要完整结果的核心排名 API 仍一次追加后全量排序，不承担额外堆维护。

### 验证

- 1,000 项目和 5,000 条证据只建立一次共享索引；周报仍计算全部项目的复航完整度，关注清单保持四项，而首页排名返回 12 项且最大的最终排序数组仅为 12。

## 0.120.0 · 2026-08-28

### 网络响应与缓存克隆隔离

- Service Worker 把响应克隆和后台缓存调度收拢到独立的尽力而为边界；宿主拒绝克隆响应体时，不再进入网络失败分支或回退旧缓存。
- 导航和应用模块的有效网络响应均优先原样返回；克隆、缓存打开、写入或配额失败都只影响可再生的运行时缓存副本。
- 既有 4 秒网络超时、750 ms 缓存打开上限、客户端取消转发和显式离线 503 语义保持。

### 验证

- 令成功导航响应与模块响应的 `clone()` 抛错；两者仍返回新网络正文、未返回预缓存旧内容且无计时器泄漏。完整 Service Worker 生命周期与发布资源契约继续通过。

## 0.119.0 · 2026-08-28

### 跨日常驻页面刷新

- 界面记录本地日签名；每秒轻量计时检查在自然日变化时触发一次保留对话框状态的重绘，“今日轨迹”、七日航迹、离开天数和问候语不会停留在昨天。
- 工作区计数、当前复航卡、首页推荐/周报和会话健康在同一次渲染中共享同一个 `now` 快照，避免午夜附近同屏组件跨越不同日期边界。
- 页面隐藏时跳过计时 DOM 查询和健康计算；标签重新可见时立即补一次刷新检查。若用户正在输入，跨日/健康重绘仍延迟并在后续周期自动重试。

### 验证

- 本地 23:59:59.999 与次日 00:00:00 生成不同日签名，无效参考返回稳定标记；界面契约覆盖隐藏跳过、可见恢复、双签名失效和共享渲染时间。

## 0.118.0 · 2026-08-28

### 符合输入意图的项目筛选

- 快捷项目筛选有输入时先按精确名称、名称前缀、名称包含、说明/下一步包含排序，再在同等级内保留上下文偏好；旧选择不再压过用户刚键入的精确项目名。
- 查询与项目字段统一采用 NFKC 兼容规范化和中文区域小写化，全角拉丁字母、大小写与兼容字符可稳定匹配。
- 空查询仍优先当前项目、活动会话项目和已选项目，并以最近活动补齐；有界 40 项窗口和确定性标题/ID 并列规则不变。

### 验证

- 用说明命中的旧偏好项目与精确/前缀/标题包含项目竞争全角大写查询；输出严格按四级匹配质量排列，规范化查询为小写半角文本。

## 0.117.0 · 2026-08-28

### 回退备份的真实字节边界

- 不支持字节流、只能调用 `file.text()` 的浏览器现在会按 Unicode 码点单遍计算实际 UTF‑8 字节数，并要求它与文件声明大小一致。
- 声明未超 25 MB 但实际文本超限、声明过小或声明异常偏大的回退文件都会在 JSON 解析前拒绝，行为与流式路径一致。
- 兼容文本解码器剥离 UTF‑8 BOM 的标准行为：只有声明大小恰好比已解码文本多 3 字节时允许差异，不放宽其他不一致。

### 验证

- 多字节中文/emoji JSON 以真实编码大小通过；大小少报、多报分别拒绝，3 字节 BOM 差异通过。既有流式精确大小、取消、非法 UTF‑8 和异常分块验证继续通过。

## 0.116.0 · 2026-08-28

### 有界的复航资源选择

- 项目资源不再先收集并全量排序所有含链接证据；项目说明、轨迹和检查点在扫描时直接解析链接，只维护最终最优的 20 个唯一 URL。
- 当前窗口以 URL 索引去重；若稍后遇到同 URL 的更新证据，或同时间下来源更可靠、写入更晚的证据，会替换旧候选并重新进入正确位置。
- 日期降序、检查点优先于轨迹/项目、同源后写入优先、单条证据内链接原序和全局 20 条上限保持不变；项目查找也不再调用源数组 `find()`。

### 验证

- 49,999 条唯一链接位于一个 50,000 记录边界工作区时，只返回最新 20 条；禁用源数组 `filter()`、`map()`、`sort()`、`find()` 后仍通过。
- 同 URL 依次出现在项目、轨迹、检查点时，最终来源升级为检查点且只占一个窗口位置。

## 0.115.0 · 2026-08-28

### 流式归档舱窗口

- 归档舱对全体项目单遍计数，只把当前 12 项分页窗口保留到内存；不再先 `filter()` 复制全部归档项目再 `slice()`。
- 继续显示项目时复用工作区计数快照取得未归档/归档总量，不再为一次按钮操作重新建立同状态项目数组。
- 输入顺序、每次扩展 12 项、准确总数/剩余数、空归档舱和可见卡片的批量复航投影保持不变。

### 验证

- 在 50,000 个交错状态项目上禁用源数组 `filter()`、`map()` 和 `slice()`；归档与未归档总数均为 25,000，首屏/扩展窗口准确且只保留 12/24 项。

## 0.114.0 · 2026-08-28

### 共享的项目详情投影

- 未归档项目详情使用一张带统计快照的复航卡；完成会话、检查点、决定、阻塞和轨迹总数在既有目标项目索引循环中同步累计，不再为统计面板重新全量扫描三类记录。
- 同一次渲染把这张复航卡同时交给项目正文与快速检查点复核对话框；归档详情同样共享自己的卡，移除原先页面与对话框的重复索引构建。
- 首页批量推荐仍使用不带统计对象的轻量卡片，避免为数万项目分配页面详情专用数据。

### 验证

- 带统计复航卡与独立统计函数的六项输出完全一致；监测源集合迭代次数时，会话、轨迹与检查点各只遍历一次。
- 界面契约确认项目路由只建立一次当前复航卡，统计面板与复核对话框均消费该共享结果。

## 0.113.0 · 2026-08-28

### 有界的快捷项目选择

- 跨项目快捷记录不再在每次页面渲染时为全部未归档项目生成选项；初始窗口只保留 40 个候选，优先当前项目、活动会话项目和已选项目，再按最近活动补齐。
- 新增项目筛选输入，可按名称、说明或下一步定位窗口外的任意未归档项目；精确名称、名称前缀、名称包含和上下文命中依次排序，并以最近活动、标题、ID 形成确定性并列顺序。
- 匹配数量通过礼貌实时区播报；无匹配时原生选择框禁用，默认记录文本焦点和会话中标记保持不变。

### 验证

- 在 50,000 项目边界禁用源数组的 `filter()`、`map()`、`sort()`、`some()` 与 `find()`，仍能找到输入末尾的唯一项目，返回窗口不超过 40 项。
- 应用内浏览器隔离验证默认焦点、候选可访问名称、单项目匹配和空结果禁用状态；没有写入测试数据，验证标签页已关闭。

## 0.112.0 · 2026-08-28

### 流式导入差异摘要

- 导入预览以固定集合顺序直接累计变化与排列差异，不再通过 `Object.entries()`、`filter()`、`map()` 和 `Object.values().some()` 建立短期汇总数组。
- 同 ID 集合的顺序比较改为显式索引循环；当前和备份中的活动会话描述也直接扫描项目与会话，不依赖源数组的 `some()` 或 `find()` 方法。
- 新增、移除、字段更新、顺序变化、设置变化与当前/备份活动会话提示保持原有语义，项目可见明细继续严格限制为每类六条。

### 验证

- 为当前工作区四个源集合安装会抛错的 `some()`，并为项目和会话安装会抛错的 `find()`；内容相同的导入预览仍准确报告无变化，并正确解析活动会话的项目标题与意图。

## 0.111.0 · 2026-08-28

### 一致的工作区计数快照

- 新增单遍工作区计数投影，一次汇总未归档/活动/暂泊/受阻/归档项目、今日轨迹和检查点总数；侧栏、顶栏与首页仪表盘共享同一结果。
- “今日”边界从一次渲染的同一本地日期快照计算，轨迹只做毫秒区间比较；不再为每条轨迹构造当前 Date，也不会在午夜跨越时让同屏入口与指标采用不同参考。
- 移除侧栏和首页五次项目 `filter()` 及今日轨迹结果数组，空工作区与快捷记录可用性继续由同一未归档计数决定。

### 验证

- 禁用项目与轨迹源数组 `filter()`，本地日首尾轨迹仍计入、次日零点排除，四种状态与检查点数字准确；无效参考时间安全给出零条今日轨迹。

## 0.110.0 · 2026-08-28

### 时间参考失效保护

- 用统一时间戳解析器校验经过时长、相对时间和离开天数的目标与参考端；数字、Date 与日期字符串继续兼容。
- 无效或非有限参考时间不再让相对文案误退化成绝对日期，也不让离开天数泄漏 `NaN`；分别安全返回“时间未知”和 0。

### 验证

- 新增无效字符串、`Infinity` 与 Date 对象参考时间测试；分钟/小时/日边界、未来钳制、分数天与复航陈旧度排序继续通过。

## 0.109.0 · 2026-08-28

### 确定性项目推荐

- 推荐项目在分数与最后活动时间完全相同时，以代码单元 ID 作为最终并列规则；首页首选不再随等价备份的项目数组顺序改变，也不依赖宿主区域排序。
- 未归档项目 ID、复航卡和推荐字段改为单遍追加，不再串联源项目 `filter()` 与两轮 `map()`/`filter()` 投影；完整排序和全部卡片输出语义保持。

### 验证

- 以反向 Unicode ID 提供完全同分同时间项目，并令源项目数组的 `filter()`/`map()` 抛错；推荐仍按代码单元顺序稳定返回且归档排除不变。

## 0.108.0 · 2026-08-28

### 备份流失败收拢

- 流式备份读取只有在完整校验字节数、完成 UTF‑8 尾部解码并组装文本后才标记成功；其余所有退出路径统一尽力取消底层读取，再释放读取锁。
- 取消操作增加一次性门闩，主动中止、容量异常与最终清理不会重复调用 `reader.cancel()`；取消拒绝和解锁异常继续不覆盖原始用户错误。

### 验证

- 非法 UTF‑8 与截断多字节序列现在均确认触发底层流取消，同时仍稳定报告“无法读取备份文件”；合法跨块 Unicode、声明大小和分块上限保持。

## 0.107.0 · 2026-08-28

### 下载清理隔离

- 备份下载的临时链接移除与对象 URL 回收分别进入尽力清理边界；任一浏览器清理 API 抛错都不再阻止另一项清理。
- 成功点击不会因事后移除/回收失败被误报，点击、链接创建或调度的原始错误也不会被清理异常覆盖；延迟回调自身同样隔离回收异常。

### 验证

- 模拟链接移除和 URL 回收同时抛错，成功路径仍正常返回且延迟回收被尝试；点击失败路径继续报告原始点击错误。

## 0.106.0 · 2026-08-28

### 有界应急停靠证据

- 快速停靠在一次目标会话轨迹扫描中用标量选择最新摘要和下一步，不再筛选、包装并全量排序全部证据。
- 未决事项只用最差项在根的有界堆保留足以确定 800 字符输出前缀的最多 401 条最新证据；拼接一旦越过持久化上限即停止，避免长会话先制造巨型中间字符串。
- 活动会话不变量与快速检查点复核改为单遍收集/择新；同毫秒后写入、畸形时间保守落后、项目/会话隔离与占位回退语义保持。

### 验证

- 在项目+会话+49,998 条同毫秒未决轨迹的 50,000 记录边界禁用 `map()`/`filter()`，停靠仍生成精确 800 字符带省略号文本，最终排序数组固定为 401 条。

## 0.105.0 · 2026-08-28

### 流式复航卡派生

- 单张复航卡在一次轨迹循环中同时选取最新摘要/下一步、三条未决、两条决定、三条置顶与三条检查点后变化，不再对长项目历史连续过滤五次。
- 内部项目证据数组先反转再稳定按时间排序，直接保留同毫秒后写入优先，不再建立“记录+插入序号”包装数组及二次映射；固定三项证据择新、项目活动日期与完整度也改为标量比较。
- 批量卡片输出用单遍追加，开放问题文本直接拼接；现有卡片字段、上限和排序语义保持。

### 验证

- 在 49,999 条同毫秒项目轨迹上禁用数组原型 `map()`/`filter()`，仍精确选出最后写入的摘要、决定、置顶、变化与最近五条；完整复航与洞察回归通过。

## 0.104.0 · 2026-08-28

### 有界注意力甲板

- 注意力甲板直接遍历已有复航卡，不再另建项目数组和卡片 `Map`；用最差项在根的有界堆只保留界面最终展示的 K 项。
- 风险分数、最久未活动与代码单元 ID 的既有确定性顺序保持，零上限立即返回，不再为所有风险项目全量排序后切片。

### 验证

- 5,000 个同分风险项目以逆序 ID 输入时仍精确返回前四项，并确认最终排序数组长度只有 4；健康/归档排除、风险原因与显式零上限继续通过。

## 0.103.0 · 2026-08-28

### 单遍周报聚合

- 周报在一次会话循环中同时累计专注时长、封顶数、打断、快速停靠、活动数与临近切换，不再对窗口数组做三次额外过滤。
- 轨迹创建、决定与解决数合并为一次扫描；可恢复度和最投入项目改为标量归约，不再复制并排序项目分钟表。活动项目 ID 也不再串联 `filter().map()`。

### 验证

- 禁用项目、会话和轨迹源数组的 `filter()` 后，窗口交叠、项目切换、全部计数、代码单元并列规则与共享复航索引仍保持精确。

## 0.102.0 · 2026-08-28

### 流式搜索索引

- 搜索索引在遍历项目时同步建立项目引用表，不再先为全部项目投影键值对数组。
- 项目与检查点的检索文本改为按字段直接拼接，不再为每条记录创建字段数组并调用 `filter()`；索引快照、分隔符与空字段语义保持。

### 验证

- 50,000 条索引构建时让项目集合的 `map()` 与数组原型的 `filter()` 主动抛错，完整索引、稀疏搜索和宽查询仍通过。

## 0.101.0 · 2026-08-28

### 有界搜索排名

- 全局搜索改为单遍扫描候选、用最差项在根的有界堆只保留最终 K 条；短词命中整个工作区时不再复制全部结果、全量排序后再截断。
- 分数、时间和代码单元 ID 的既有确定性顺序保持，结果对象只为最终返回窗口物化，调用方 100 条硬上限不变。

### 验证

- 50,000 个候选中的 49,999 条同时命中宽查询，逆序输入仍精确返回确定的前 40 条，并确认最终排序数组长度仅为 40；稀疏查询与索引快照语义继续通过。

## 0.100.0 · 2026-08-28

### 有界时间线选择

- 项目时间线改为单遍统计全量、用有界堆只保留当前窗口最新 K 条，首屏不再为目标项目的全部历史建立包装数组并全量排序。
- 合法同毫秒记录的后写入优先、畸形时间稳定顺序、逐页扩展总数与源数组不变语义保持。

### 验证

- 大历史回归提升到 50,000 条合法递增轨迹，确认首屏精确返回最新 30 条、剩余 49,970 条，并且最终排序数组长度仅为 30。

## 0.99.0 · 2026-08-28

### 稀疏资源历史

- 项目详情在读取时间戳、拼接检查点字段和分配排序条目前先跳过不含 HTTP(S) 候选的证据；常见的长历史不再为无链接记录建立排序副本。
- 单条文本的 URL 扫描由一次性匹配数组改为惰性迭代，达到资源上限后不会继续物化后续候选；大小写不敏感语义保持。

### 验证

- 新增 50,000 条无链接记录加末条大写 `HTTPS://` 资源测试，并用抛错时间戳证明无关记录未进入排序阶段。

## 0.98.0 · 2026-08-28

### 客户端取消传播

- Service Worker 把原始导航或外壳请求的取消信号转发到内部 4 秒网络控制器；标签关闭、导航替换或调用方取消时立即中止联网。
- 客户端主动取消不再被当作离线故障读取缓存并合成响应，真正超时和网络失败仍保持完整离线回退；监听器与计时器在所有路径释放。

### 验证

- 新增原始请求取消测试，确认内部信号收到取消、响应保留 `AbortError`、生命周期正常结束且无计时器残留。

## 0.97.0 · 2026-08-28

### 低分配导入预览

- 同 ID 的规范化记录改为按自有扁平字段逐项比较，不再为每条项目、会话、轨迹和检查点各序列化两份 JSON 字符串。
- 项目集合的新增、移除、更新、未变、顺序变化与可见名称明细共享一次索引遍历，移除第二对集合等长 `Map`。

### 验证

- 新增禁用 `JSON.stringify` 的相同工作区预览测试，并保留项目计数、顺序变化和六条明细上限回归。

## 0.96.0 · 2026-08-28

### 流式状态规范化

- 状态规范化改为单遍构建项目、会话、轨迹和检查点输出，并在同一遍建立必要 ID 索引；不再串联 `filter().map()` 或为索引额外投影 ID 数组。
- 孤儿记录过滤、悬空会话引用清除、记录顺序和既有缺省语义保持不变，降低 50,000 条导入在原始 JSON、校验和克隆并存时的瞬时内存峰值。

### 验证

- 新增四类源数组主动拒绝 `map()` / `filter()` 的规范化测试，覆盖合法交叉引用与输出顺序。

## 0.95.0 · 2026-08-28

### 应急停靠入口

- 活动会话可用 `Ctrl/⌘ Shift S` 从任意非模态页面立即停靠，复用既有原子事务生成低置信度检查点；没有活动会话时不拦截浏览器默认动作。
- 搜索空态的快捷动作面板会在可执行时展示“应急停靠”，三个重复停靠入口都增加项目上下文名称和快捷键提示。
- 模态边界仍优先于所有全局快捷键，编辑、确认或导入期间不会误收拢后台会话。

### 验证

- 新增快捷键、命令入口、模态顺序和可访问名称契约测试，并保留原有快速停靠状态事务回归。

## 0.94.0 · 2026-08-28

### 大工作区校验峰值

- 基础结构校验也采用 50 条诊断预算，达到上限后追加一次截断说明并停止遍历，避免大量缺失或重复 ID 扩成数万条错误字符串。
- 导入引用完整性改用项目、会话、检查点三个单遍 `Map`；移除 3 个等价 `Set`、6 个集合等长临时数组，以及活动会话计数的筛选数组。

### 验证

- 新增结构错误封顶测试，并让四个记录集合主动拒绝 `map()` 调用，确认合法交叉引用只依赖流式索引构建。

## 0.93.0 · 2026-08-28

### 资源证据一致性

- 复航资源在证据时间相同时显式采用“可靠检查点、随记、项目字段”的来源优先级，同类证据以后写入者优先；重复链接不再可能被较弱的项目字段或较早记录抢占出处。
- 畸形时间统一落到保守基线后仍使用相同确定性顺序，不依赖 JavaScript 稳定排序偶然保留的数组位置。

### 验证

- 新增相同合法时间与畸形时间两轮资源排序测试，覆盖跨来源优先级、同类后写入优先与 URL 去重后的来源元数据。

## 0.92.0 · 2026-08-28

### 无损数据边界

- schema v1 的根、元数据、设置、界面状态与四类记录新增明确字段白名单；未知字段不再一部分静默丢弃、一部分以未深冻结嵌套对象混入运行态，而是列出所属对象后原子拒绝。
- 规范化器也只拣取已知元数据和设置字段，作为绕过常规校验调用时的纵深防护；缺省字段仍由 schema 默认值补齐。

### 验证

- 新增八个容器/记录层级的未知字段测试，并覆盖嵌套设置对象在本地提交前拒绝、内存与主键保持原样。

## 0.91.0 · 2026-08-28

### 设置一致性

- 离开提醒阈值的模型边界从“有限数”收紧为 1–365 的安全整数；备份、跨标签页数据和本地更新不再接受 `1.5` 天或非安全数值。
- 核心错误信息明确说明整数要求，与设置页自身的校验和离散天数选项保持一致。

### 验证

- 新增 0、1.5、366、`NaN`、无限值与文本拒绝，以及 1/7/365 边界接受测试；本地提交失败继续保持主键与内存对象不变。

## 0.90.0 · 2026-08-28

### 归档复航

- 每张归档卡新增“查看现场”入口，可先进入现有只读详情核对最后状态、下一动作和完整历史轨迹，再决定是否恢复项目。
- 查看与恢复分别带有有界项目上下文可访问名称；卡片动作在窄宽度下自动换行，不挤压轨迹数量。

### 验证

- 新增归档卡详情路由、上下文名称与只读详情提示契约测试。

## 0.89.0 · 2026-08-28

### 地址可靠性

- 前端路由在替换、分段和百分号解码前先限制完整哈希为 2,420 单元，超长畸形地址立即进入未找到页，不再制造与输入规模成正比的分段数组。
- 上限仍覆盖 200 单元项目 ID 的最坏常用 UTF-8 百分号编码；合法中文 ID、编码斜杠与三个顶层路由保持兼容。

### 验证

- 新增最大合法中文项目 ID 与成千上万路径段的总哈希预算测试。

## 0.88.0 · 2026-08-28

### 多标签页可靠性

- 写租约现在只把“当前时刻之后且不超过完整 5 秒租期”的值视为活动；损坏存储或系统时钟回拨造成的超远未来期限不再永久锁死工作区。
- 正常短租约、同步争抢和提交前过期检查保持不变；接管超界租约后仍核对唯一所有者并只释放自己的令牌。

### 验证

- 将活动外部租约收紧为真实租期内样例，并新增超远未来租约可安全接管、主键成功提交与令牌清理测试。

## 0.87.0 · 2026-08-28

### 资源安全

- HTTP(S) 资源提取在中文逗号、顿号、冒号、书名号、全角括号等正文边界停止，不再把“链接、下一步”整段误编码成另一个国际化主机。
- 对同一主机标签内混合 ASCII 字母数字与非 ASCII 字符的无分隔歧义输入保守拒绝；纯国际化标签、国际化标签加 ASCII 顶级域及中文路径继续支持。

### 验证

- 新增中文正文标点、无标点粘连、纯 IDN、混合层级 IDN 与中文路径的规范 URL 投影测试。

## 0.86.0 · 2026-08-28

### 容量与可靠性

- 流式备份读取现在核对声明大小与实际字节总数，声明偏小不再继续解码，声明偏大也不会把截断内容送入 JSON 解析。
- 拒绝空字节块与超过 16,384 块的异常碎片流；已解码文本按约 64K 段合并，避免极碎输入制造海量字符串数组条目。

### 验证

- 新增大小少报/多报、无进度空块与持续单字节分块测试；异常路径取消读取器，合法跨块 UTF-8 仍保持无损。

## 0.85.0 · 2026-08-28

### 可靠性

- 本地静态服务拒绝路径部分含百分号编码的非规范资源别名；`/src%2fmain.js`、`/%69ndex.html` 等不再出现线上可访问、离线外壳无法命中的分裂行为。
- 查询参数仍可正常使用，项目哈希路由中的编码 ID 继续由前端独立解析，不受静态路径约束影响。

### 验证

- 新增编码斜杠、编码文件名字符和编码扩展名的解析/真实 HTTP 拒绝测试，并确认拒绝后首页仍可访问。

## 0.84.0 · 2026-08-28

### 体验与确定性

- 搜索输入法组合期间不再对拼音/注音等中间态反复扫描工作区；组合完成后的正常 `input` 才刷新结果，减少大工作区中文输入卡顿。
- 搜索同分结果、周报同时间会话/同投入项目和关注清单最终并列项改用明确 UTF‑16 码元排序，不再受主机区域设置与 ICU 版本影响。

### 验证

- 新增 Unicode ID（`z` / `ä`）并列顺序与输入法组合短路契约测试。

## 0.83.0 · 2026-08-28

### 可靠性

- 本地 HTTP 服务为请求目标增加 8,192 单元硬上限，超界在 URL 构造/解码前返回 414，并使用 `no-store` 安全错误响应；后续合法请求不受影响。
- 只有真正缺省的 `null/undefined` 解析为根路径；空字符串、数字等非 origin-form 输入不再因假值回退意外获得首页，而是返回 400。

### 验证

- 新增解析器空值/非字符串/长度边界与真实 HTTP 414 后存活契约测试。

## 0.82.0 · 2026-08-28

### 可靠性

- Service Worker 激活时的旧壳缓存枚举与逐项删除改为尽力而为；临时权限/存储故障不会再拒绝整个激活生命周期。
- 新壳仍必须在安装期原子完整预缓存；一旦安装成功，激活会继续 `clients.claim()` 并提供网络服务，即使旧缓存暂时无法清理。

### 验证

- 新增缓存列表拒绝、单个旧缓存删除拒绝两条生命周期契约，均要求激活完成并接管客户端；正常清理与第三方缓存保留测试继续通过。

## 0.81.0 · 2026-08-28

### 可靠性

- 备份字节流改用 fatal UTF-8 解码，非法续字节、错误序列或文件末尾截断不再被静默替换为 `�` 后继续导入。
- 流式解码仍跨块保留多字节字符状态，中文和 emoji 即使逐字节分块也能无损恢复；错误统一走稳定的“无法读取备份文件”反馈。

### 验证

- 新增逐字节拆分中文/emoji、非法双字节序列及截断四字节序列契约测试。

## 0.80.0 · 2026-08-28

### 性能

- 备份 FNV-1a 校验改为直接把 JSON UTF-16 码点编码成 UTF-8 字节并逐字节哈希，不再额外分配一份完整 `TextEncoder` 字节数组；ASCII、中日韩文本、emoji 与转义字符保持原校验值。
- 导入项目差异在扫描时只保留前 6 条新增/删除/更新细节，总数独立累计；ID 映射改为直接填充，不再创建全量条目数组或先生成全部摘要再切片。

### 验证

- 新增与原 `TextEncoder` 算法逐例对照及禁用编码器契约，覆盖 emoji、孤立代理转义、换行与引号；既有差异详情上限与总数测试继续通过。

## 0.79.0 · 2026-08-28

### 性能

- 项目时间线在扫描时先筛目标项目，只为匹配轨迹分配排序条目；大工作区中的无关轨迹不再产生临时包装对象。
- 项目统计改为会话、轨迹、检查点各一次流式遍历，同时累计总数、完成会话、决定与阻塞，不再用多层 `filter` 重扫。

### 验证

- 新增 50,000 条无关轨迹加 2 条目标轨迹测试，并禁用集合 `map`/`filter` 作为性能契约，证明全量中间映射和统计重扫已移除。

## 0.78.0 · 2026-08-28

### 性能

- 复航索引新增目标项目集合：单项目页和归档窗口仍线性检查引用，但只收集、读取时间并排序实际请求项目的证据，不再为无关项目构造中间数组。
- 首页排名显式索引全部未归档项目，归档记录不会混入派生成本；批量 API 保持输入顺序、缺失 ID 忽略与共享一次扫描的契约。

### 验证

- 新增带时间字段访问探针的隔离测试，证明请求 `p1` 时不会读取或排序 `p2` 的证据载荷，只有明确请求后才访问。

## 0.77.0 · 2026-08-28

### 性能

- 归档舱当前窗口改为批量构建复航卡，一份索引服务最多 12 张卡，不再按卡重复扫描全部项目、会话、轨迹和检查点。
- 可见归档卡的轨迹数量在一次遍历中汇总；摘要增加防御性缺省，局部损坏不会让整个归档舱白屏。

### 验证

- 新增归档批量投影、单次轨迹计数及旧逐卡过滤路径移除契约。

## 0.76.0 · 2026-08-28

### 可靠性

- 60 秒复航卡不再丢失只写在检查点自由文本里的未决线索：没有实时问题/阻塞时，以“检查点曾记录（待确认）”显示，不把历史描述冒充当前事实。
- 实时未解决信号继续覆盖旧检查点文本；快速停靠的“未记录”占位不作为历史线索展示，屏幕与复制简报共享这一语义。

### 验证

- 新增检查点独有文本、实时信号覆盖、快速停靠占位抑制，以及屏幕/简报一致性契约测试。

## 0.75.0 · 2026-08-28

### 性能

- 首页改用单次工作区总览投影：一份复航索引与卡片同时驱动项目排名、七日航迹完整度和关注清单，不再为三个区域重复扫描全部证据。
- “显示更多项目”先按状态计数并直接扩展窗口，不再为了判断剩余数量提前构造、排序整套复航卡；最终渲染仍保持原排序和焦点落点。

### 验证

- 新增 1,000 项目/5,000 轨迹共享索引契约，以集合迭代次数证明三个首页区域只建立一次证据索引。

## 0.74.0 · 2026-08-28

### 修复

- 七日航迹改为按会话区间与统计窗口是否相交筛选；窗口前开始、窗口内结束或仍在活动的会话不再整段漏计。
- 专注时长只累计落在窗口内的交集，再应用单段 12 小时上限；会话数、应急停靠/中断、近距切换和投入最多项目共享同一相交集合。

### 验证

- 新增跨越窗口起点、恰在边界结束、窗口内项目切换及超龄活动会话契约测试。

## 0.73.0 · 2026-08-28

### 可靠性

- 全局搜索新增 500 单元查询、24 个去重词元与 100 单元单词元预算；界面声明同一输入上限，脚本绕过或规范化膨胀也会整次拒绝，避免大工作区被恶意粘贴放大计算。
- HTTP(S) 资源提取在去重前移除 DNS 主机尾点，`example.com.` 与 `example.com` 不再重复占位；空主机变体继续拒绝。

### 验证

- 新增查询总长、词元数量/长度、NFKC 后长度、重复词元及尾点主机规范化契约测试。

## 0.72.0 · 2026-08-28

### 无障碍

- 对话框因外部标签页更新或会话生命周期边界而重绘时，现在会恢复取消、保存、确认等按钮焦点，不再只照顾输入控件。
- 字段值/选区与全部可聚焦控件位置分别记录并按类型恢复；导入预览纳入同一机制，消失或禁用目标不会被强制聚焦。

### 验证

- 新增按钮焦点、字段选区、导入预览及失效目标保护契约测试。

## 0.71.0 · 2026-08-28

### 无障碍

- 主题与动态效果的分段按钮现在位于有名称的 `group` 中，读屏可识别其共同语义。
- 重复的解决/重开、置顶/取消置顶、归档恢复和简报复制动作加入所属证据或项目上下文；动态名称折叠空白并以 80 字符 Unicode 安全压缩，避免冗长或拆分 emoji。

### 验证

- 新增分组角色、上下文化动态名称与长度压缩契约测试。

## 0.70.0 · 2026-08-28

### 修复

- 活动会话页面持续打开跨过 12 小时或本地午夜时，陈旧会话警示会随计时生命周期自动出现，不再依赖刷新、导航或其他状态变化。
- 生命周期检查用会话/陈旧原因/确认态签名去重，只在边界变化时重绘；焦点位于输入控件时延后，避免清空未提交草稿，对话框沿用瞬态恢复。

### 验证

- 新增生命周期签名、去重重绘、输入焦点保护与对话框保留契约测试。

## 0.69.0 · 2026-08-28

### 可靠性

- 本地提交新增 5 秒同源写租约：跨标签页在读取基线、主写与配额重试前核对唯一所有者，关闭“同时读到旧基线后都写同一修订号”的竞态窗口。
- 租约带随机所有者与绝对过期时间；主写前同时复核所有权与活性，页面暂停超过租期后不得继续写；正常结束只释放自己的令牌，崩溃遗留租约可自动接管。

### 验证

- 新增同步重入的双标签页争抢、活动外部租约拒绝、过期租约接管及提交后清理测试。

## 0.68.0 · 2026-08-28

### 修复

- 导入预览现在绑定首次完整校验生成的规范化快照与信封来源证据；旧格式缺省时间不会被更晚时钟重新填充，差异刷新也不会丢失版本、导出时间或校验结果。
- 工作区在预览期间变化时，只重新计算它与同一来件快照的差异；最终提交元数据仍按确认时刻单调前进。

### 验证

- 新增旧格式缺省项目时间跨预览时钟保持不变，以及界面始终保存规范化快照的契约测试。

## 0.67.0 · 2026-08-28

### 可靠性

- 备份导入优先使用 Blob 原生字节流，逐块复核实际大小，超过 25MB 时在解码前立即取消；不再只信任读取前的文件元数据。
- 后选文件会主动中止前一个读取，应用销毁也会取消在途导入；旧浏览器的 `text()` 回退在 JSON 解析前无额外大缓冲地复核 UTF-8 字节数。

### 验证

- 新增流式 JSON、声明/实际大小不一致、超界取消、预中止及界面控制器生命周期测试。

## 0.66.0 · 2026-08-28

### 可靠性

- 静态响应现在先打开文件描述符，再从同一描述符确认文件类型与长度，并固定流读取上界；路径替换或文件增长不会越过已声明的 `Content-Length`。
- HEAD、零字节、打开失败、非文件与客户端中断路径显式关闭描述符，避免长期运行时泄漏句柄。

### 验证

- 新增独立临时根的零字节 GET/HEAD 网络测试；临时文件在测试结束时立即清理。

## 0.65.0 · 2026-08-28

### 安全与可靠性

- 本地 HTTP 服务现在只接受 control-free origin-form 请求目标；绝对 URL、网络路径、片段、原始或解码 C0/C1 控制字符统一返回 400，避免 URL 解析器替调用方静默修正畸形输入。
- 合法查询参数仍可用于模块缓存破坏，但不参与静态文件映射；拒绝畸形请求后服务继续可用。

### 验证

- 新增解析层与真实 HTTP 层的绝对目标、片段、空白及百分号编码控制字符回归测试。

## 0.64.0 · 2026-08-28

### 修复

- 在线导航与外壳资源不再等待运行时缓存打开；网络内容立即交付，fetch 事件在同步分发阶段先登记聚合寿命，稍后产生的缓存刷新安全纳入其中，避免延迟调用 `waitUntil()`。
- CacheStorage 打开永久挂起时以 750ms 为上限收敛，离线回退与工作线程都不会无限等待或遗留计时器。

### 验证

- 新增缓存打开永久不决、网络立即成功、事件寿命收敛与计时器清理测试。

## 0.63.0 · 2026-08-28

### 修复

- Service Worker 运行时打开或读取 CacheStorage 被拒绝时，不再让可用的在线导航与模块请求一起失败；网络响应现在始终优先交付。
- 网络与缓存同时不可用时返回明确 503；安装期仍坚持原子完整预缓存，残缺离线外壳不会被发布。

### 验证

- 新增运行时缓存打开拒绝、读取拒绝、在线旁路与离线显式失败测试。

## 0.62.0 · 2026-08-28

### 修复

- 复制简报的“未决事项”现在优先采用实时未解决的问题/阻塞，避免旧检查点覆盖检查点后新增信号；只剩检查点自由文本时明确标为历史待确认，避免把已解决事项冒充当前事实。

### 新增

- 简报新增“证据状态”，携带夹取到 0–100% 的复航完整度及当前可执行证据缺口，外发后仍保留置信度语境。

### 验证

- 新增实时未决优先、检查点历史标记、无未决默认语义、完整度与缺口格式测试。

## 0.61.0 · 2026-08-28

### 修复

- 旧浏览器的复制回退现在会记录原焦点与文本选区，并在成功或失败后以不滚动方式尽力恢复，避免隐藏文本域让键盘用户失去位置。
- 临时节点清理失败、原控件在异步期间移除或禁用时不再遮蔽已经完成的复制结果。

### 验证

- 新增焦点/选区恢复及清理与聚焦双重异常隔离测试。

## 0.60.0 · 2026-08-28

### 新增

- 项目 60 秒复航卡新增“复制简报”，在本机生成包含当前状态、第一动作、未决事项和复航提示的结构化纯文本，可直接带入邮件、聊天或笔记。
- 优先使用 Clipboard API；权限拒绝或不支持时回退到短生命周期隐藏文本域，所有路径都清理临时节点。

### 可靠性

- 五个简报字段各自折叠为单行并按对应模型上限压缩，保持完整 Unicode 码点；并发复制只让最新请求反馈，应用销毁后旧异步完成不再更新界面。
- 新模块同步加入本地服务器白名单、语法检查和完整离线外壳。

### 验证

- 新增简报格式/容量/单行结构、现代剪贴板、拒绝后回退、无能力、回退拒绝与节点清理测试，并加入界面异步门闩契约。

## 0.59.0 · 2026-08-28

### 修复

- 状态订阅者抛错不再从已完成的持久化提交中逸出，因此调用者不会在主数据实际保存后收到“操作失败”的假象。
- 单个故障订阅不阻断同轮其他订阅者；订阅集合按发送开始时的快照遍历，回调期间增删监听不会改变当前通知批次。
- 同一故障订阅只排入一次可见诊断，后续仍允许它重试；非函数订阅在注册时明确拒绝。

### 验证

- 新增连续两次已提交写入、故障/健康订阅并行、冻结状态投递、诊断去重和非法订阅测试。

## 0.58.0 · 2026-08-28

### 修复

- `AppStore` 发布的状态现在冻结根、元数据、设置、界面状态、四个集合和每条扁平记录，调用者无法绕过 `update()` 直接改写内部状态。
- 冻结发生在成功持久化后，下一次更新仍从不可变快照结构化克隆；读取和订阅继续共享稳定身份，不为 50,000 条工作区制造每次渲染的深拷贝。

### 验证

- 新增根/容器/记录冻结、字段与数组旁路写入失败、持久化原文不变，以及后续正规配方仍可提交的测试。

## 0.57.0 · 2026-08-28

### 修复

- 浏览器因隐私策略、来源权限或安全设置拒绝读取 `localStorage` 时，应用不再在构造阶段整页崩溃，而是带明确通知进入不可保存的临时空白工作区。
- 运行期间的跨标签读取失败保持当前内存状态并产生可见通知；保存前无法核对现有主键时原子拒绝，不冒险覆盖未知数据。
- 主数据校验失败后若滚动快照也不可读，恢复流程安全降级而不让第二次存储异常逸出。

### 验证

- 新增启动即拒绝、运行后撤销读取权限、外部刷新与保存前核对失败测试。

## 0.56.0 · 2026-08-28

### 修复

- 工作区根、元数据、设置、界面状态以及每条项目/会话/轨迹/检查点记录现在只接受普通 JSON 对象，不再把 `Date`、`Map`、`Set` 等带特殊序列化语义的对象当成安全状态。
- 本地提交在持久化前应用同一形状检查，阻止“内存检查通过、JSON 写入变形、下次启动才损坏”的延迟故障。

### 验证

- 新增特殊根对象、容器对象、记录对象和本地 `meta = Date` 的原子拒绝测试。

## 0.55.0 · 2026-08-28

### 修复

- HTTP(S) 资源提取把 C1 控制区（`U+0080–U+009F`）纳入原文与百分号解码后的拒绝边界，不再把含 NEL 等不可见控制符的目标渲染成可点击资源。
- 项目哈希路由复用持久化 ID 的统一控制字符判定，C1、零宽与双向控制符即使经过百分号编码也稳定进入未找到页。

### 验证

- 新增原始/编码 C1 资源 URL、编码 C1 路由和编码双向覆盖路由回归测试。

## 0.54.0 · 2026-08-28

### 安全与可靠性

- 项目、会话、轨迹和检查点文本现在统一拒绝零宽空格、双向覆盖、方向隔离、字节序标记与非排版控制字符，避免粘贴或导入内容在界面中伪装真实顺序。
- 持久化 ID 同步拒绝上述不可见字符；正常换行、制表和回车仍可用于多行内容。
- 文字塑形所需的 ZWNJ/ZWJ 保持合法，阿拉伯文字连接与“👩‍💻”等组合 emoji 不受影响。
- 不可信的备份版本元数据若含不可见控制符将不显示，但不会阻断有效工作区数据恢复。

### 验证

- 新增导入、本地提交和信封元数据三条路径的控制字符测试，并确认普通排版空白与 ZWJ 组合 emoji 仍被接受。

## 0.53.0 · 2026-08-28

### 修复

- JSON 备份下载现在先把临时链接接入文档并触发点击，再延迟释放对象 URL，避免浏览器尚未接管下载时资源已经失效。
- 点击或清理调度失败时立即撤销对象 URL 并移除临时节点，不留下内存资源或隐藏链接。

### 验证

- 新增成功顺序、点击失败、调度失败与缺失浏览器能力测试；离线缓存和本地服务白名单同步纳入新模块。

## 0.52.0 · 2026-08-28

### 修复

- 搜索结果摘要与备份信封版本元数据改用统一的 Unicode 安全压缩，不再可能在 emoji 或其他代理对中间截断。
- 搜索摘要仍先折叠换行和连续空白，并严格控制在 150 个 UTF-16 单元内；备份元数据保持 80 单元上限。

### 验证

- 新增跨换行 emoji 搜索摘要与超长 emoji 备份版本测试，同时检查省略号、容量边界和完整码点。

## 0.51.0 · 2026-08-28

### 修复

- 任一模态对话框打开时，全局搜索、快捷记录、新项目、轨迹聚焦和工作区撤销快捷键不再穿透到后台或叠加第二层模态。
- Escape 等对话框原生键盘行为仍保持；关闭后全局快捷键立即恢复。

### 验证

- 新增键盘处理顺序契约，确保模态边界判断先于所有全局命令，尤其是可能改变整个工作区的撤销操作。

## 0.50.0 · 2026-08-28

### 修复

- “跟随系统”主题现在同时驱动页面 CSS 与浏览器 `theme-color`；系统在深浅色之间切换时，标题栏/安装式窗口颜色立即同步。
- 显式明亮或深色主题不受系统切换覆盖；应用销毁时移除媒体查询监听，已经排队的旧回调也会安全失效。

### 验证

- 新增有效主题纯函数矩阵、未知模式安全降级，以及系统色彩监听创建/释放契约测试。

## 0.49.0 · 2026-08-28

### 新增

- 原本只存在于数据模型的“减少动态效果”设置正式接通：默认跟随系统，也可在复航台内始终关闭平滑滚动并把非必要过渡/动画压至近零。
- 设置即时投影到根节点，随备份和跨标签同步保持一致，并提供准确的按钮状态、焦点恢复和无障碍播报。

### 验证

- 新增布尔设置持久化、设置控件、根节点投影与强制减少动效 CSS 契约测试；系统级 `prefers-reduced-motion` 支持继续保留。

## 0.48.0 · 2026-08-28

### 新增

- 数据保险箱新增“离开提醒阈值”，可选 1 / 3 / 7 / 14 / 30 / 90 天；导入的其他合法 1–365 天值也会保留并显示。
- 修改后关注清单立即按新阈值重算，设置随本地备份和跨标签同步流转，并提供可见提示与无障碍播报。

### 验证

- 新增合法数字持久化、365 天边界拒绝、控件语义和界面调用契约测试。

## 0.47.0 · 2026-08-28

### 修复

- 连续选择多个备份文件时，只有最后开始的读取可以建立导入预览；较慢的旧文件即使最后完成也不会覆盖新选择或产生过期错误提示。
- 应用销毁会使尚未完成的文件读取失效，旧实例不再尝试重绘、打开对话框或清空新实例控件。

### 验证

- 新增最新请求门闩交错测试与界面生命周期契约测试，覆盖后发请求取代先发请求和销毁失效。

## 0.46.0 · 2026-08-28

### 可靠性

- 带省略号的派生文本改为按完整 Unicode 码点累计，边界恰好落在 emoji 代理对中间时不会生成孤立字符。
- 所有表单 `maxlength` 直接引用持久化模型的统一字段常量；新项目第一动作上限由漂移的 400 字恢复为模型定义的 600 字。

### 验证

- 新增 emoji 边界与孤立代理回归测试，并以界面契约测试禁止重新引入数字硬编码的 `maxlength`。

## 0.45.0 · 2026-08-28

### 修复

- 1,200 字的完整“下一步”轨迹仍原样保留，但同步到项目动作和快速检查点时会压到 600 字并用省略号明确标记，不再被严格提交闸门卡住。
- 快速停靠摘要、下一动作和合并未决事项分别服从 1,200 / 600 / 800 字持久化边界；已解决的问题或阻塞不再重新进入快速检查点。
- 快速检查点复核的纯函数也主动拒绝四类超长字段，绕过浏览器 `maxlength` 时仍能给出准确错误。

### 验证

- 新增通用限长、完整证据与派生投影分离、快速停靠极限字段、已解决事项过滤和复核字段上限测试。

## 0.44.0 · 2026-08-28

### 可靠性

- 每次本地状态提交现在与导入、启动恢复和跨标签同步共享完整严格校验；超长文本、孤立/跨项目引用、非法枚举和因果时间错误会在写入前原子拒绝。
- 容量失败测试数据改为合法字段边界，避免可靠性测试依赖一份程序本身不应接受的状态。

### 验证

- 新增本地超长内容、孤立引用和非规范时间拒绝测试；50,000 条记录极限提交约 330 ms，隔离来源中的示例现场真实加载成功。

## 0.43.0 · 2026-08-28

### 修复

- 同一毫秒内连续写入的轨迹、检查点和会话统一采用“后写入者更新”，复核升级后的可靠检查点会立即成为复航来源。
- 开始会话与快速检查点复核对话框复用同一复航索引，不再各自维护可能分叉的最新检查点排序。
- 时间线对合法同刻记录采用后写优先，同时保留畸形日期输入的稳定降级顺序。

### 验证

- 新增同毫秒轨迹、检查点、会话与时间线顺序回归测试，并确认投影过程不改变持久化集合顺序。

## 0.42.0 · 2026-08-28

### 新增

- 快速停靠复航卡新增“复核并升级检查点”，用预填表单建立新的人工可靠检查点，同时保留原快速记录和会话关联历史。

### 可靠性

- 复核提交要求来源仍是项目最新快速检查点，并拒绝“未记录”的摘要或下一动作，过期表单不会覆盖更新后的现场。

### 验证

- 新增成功升级、状态不变、过期来源、占位字段、不可用项目、最新人工检查点及无障碍对话框契约测试。

## 0.41.0 · 2026-08-28

### 新增

- 60 秒复航卡新增可执行“复航缺口”，把完整度百分比解释为下一步应补的状态、动作、检查点、材料入口或未收拢会话核对。
- 快速停靠检查点明确列出低置信度复核要求；证据完整时不渲染空缺口面板。

### 验证

- 新增空白项目、快速停靠、缺恢复入口和检查点后活动会话四类缺口推导测试。

## 0.40.0 · 2026-08-28

### 修复

- 本地服务端口改为完整十进制严格解析，不再把 `4173abc` 等畸形环境变量宽松截断为可用端口。
- 白名单文件探测竞态安全转为 404；静态 GET/HEAD 和文本错误响应提供准确 `Content-Length`。

### 验证

- 新增端口边界矩阵、GET/HEAD 长度一致性与错误正文长度测试。

## 0.39.0 · 2026-08-28

### 修复

- Service Worker 为在线导航与外壳资源请求增加 4 秒可中止超时，弱网永久悬挂时会及时回退到最后完整缓存。
- 网络成功、失败和超时路径都会清理计时器，不积累后台资源。

### 验证

- 原生 Worker 仿真新增可控悬挂网络，验证导航和模块请求中止后均返回预缓存内容且无计时器泄漏。

## 0.38.0 · 2026-08-28

### 修复

- 严格备份新增工作区创建—更新顺序、项目自身时间窗口，以及会话/轨迹/检查点所属项目窗口校验。
- 快速停靠并立即接续时，工作区元数据时间现在推进到晚 1 ms 的新会话开始，避免生成内部时间倒序。

### 验证

- 新增六类工作区/项目时间越界测试和接续停靠元数据锚点契约测试。

## 0.37.0 · 2026-08-28

### 修复

- 哈希路由改为严格解析：未知路由、额外路径段、缺失项目 ID、畸形百分号、控制字符和超长 ID 不再静默回落首页或被截断采用。
- 未知页面现在使用准确的页面标题，并稳定显示现有“未找到”恢复入口。

### 验证

- 新增规范顶层路由、Unicode/编码斜杠项目 ID 及十类畸形哈希回归测试。

## 0.36.0 · 2026-08-28

### 修复

- 50,000 条总记录安全上限下沉为基础状态不变量，本地新增不再能越过仅由导入流程执行的容量边界。
- 超限状态在遍历记录前直接拒绝，避免异常大集合带来的额外验证开销。

### 验证

- 新增基础状态与存储更新两层超限测试，确认失败时内存状态和持久化原文均不改变。

## 0.35.0 · 2026-08-28

### 新增

- 设置页显示按 UTF-16 估算的本应用与当前来源总存储占用，并以明确标注的 5 MiB 保守参考线在 80%/100% 分级提醒备份。

### 可靠性

- 存储枚举不可用、读取抛错或参考边界无效时安全降级，不让容量提示阻断设置页。

### 验证

- 新增键值字节计数、应用前缀归属、阈值边界和不可读存储测试。

## 0.34.0 · 2026-08-28

### 修复

- 跨标签状态即使修订号前进，只要工作区更新时间早于当前状态也会被拒绝，逻辑时间单调性不再能被伪造高修订绕过。

### 验证

- 新增高修订、旧时间的结构合法外部状态回归测试，确认拒绝后保留同一当前状态对象并告警。

## 0.33.0 · 2026-08-28

### 修复

- 多标签存储监听现在处理 `key === null` 的全量清空事件，另一标签页调用 `localStorage.clear()` 后当前页面会同步到更高修订的空工作区。
- 浏览器提供 `storageArea` 时会校验事件来源，其他存储区与无关键不会触发工作区刷新。

### 验证

- 新增全量清空、错误存储区与无关键事件的回归测试。

## 0.32.0 · 2026-08-28

### 修复

- 导入替换的修订号改为 `max(当前, 备份) + 1`，旧备份不再让当前工作区版本倒退；重置也从当前修订继续而非归零。
- 跨标签同步只采用修订号严格前进的有效状态；结构合法但更旧或同修订的外部原文被拒绝并产生去重可见告警。
- 外部显式移除主数据键会生成“当前 + 1”的空工作区，保留清空语义同时维持后续覆盖保护。

### 验证

- 新增旧备份覆盖高修订、有效外部降级拒绝、外部主键移除和重置单调性测试。

## 0.31.0 · 2026-08-28

### 修复

- 严格导入验证来源检查点不得晚于接续会话开始，结束检查点不得落在会话区间之外。
- 关联会话的轨迹与检查点必须位于开始和可选结束时间之间；问题/阻塞的解决时间不得早于原记录。
- 活动会话不得已经持有结束检查点，避免“仍在计时但已经结束”的矛盾现场进入工作区。

### 验证

- 新增包含两个会话、三个检查点和三类倒序轨迹的组合备份测试，逐项核对因果错误提示。

## 0.30.0 · 2026-08-28

### 修复

- 新增逻辑时间锚点：系统时钟回拨时，工作区 `updatedAt` 不早于上一修订，恢复、替换、重置与导出同样保留单调时间。
- 新项目、会话、会话内轨迹、快捷捕获、检查点、快速停靠接续、编辑、状态、解决、航标、归档与恢复均相对各自项目/会话/证据锚点取最大时间。
- 快速停靠接续会话固定晚于生成的检查点 1 毫秒，避免回拨或同毫秒操作打乱来源顺序。

### 验证

- 新增逻辑时间辅助函数、快捷捕获未来锚点和存储元数据时钟回拨测试；无效当前时间明确拒绝。

## 0.29.0 · 2026-08-28

### 修复

- 导入与本地持久化时间只接受应用自身生成的规范 UTC ISO 字符串（固定三位毫秒与 `Z`）。
- `Date.parse` 虽可读取但跨浏览器/地区语义不稳定的自然语言、美式日期、时区偏移和省略毫秒格式现在会在归一化与写入前拒绝，避免复航排序和统计漂移。

### 验证

- 新增四类可解析但非规范时间格式的拒绝测试，并保留规范时间的完整往返验证。

## 0.28.0 · 2026-08-28

### 安全

- 复航资源新增 2,048 字符上限，并拒绝原文或解码路径/查询中的 ASCII 控制符、双向覆盖符、零宽及其他不可见格式字符；既有凭据和非 HTTP(S) 拒绝继续生效。
- 外链可访问名称明确说明会在新标签页打开，继续使用 `noopener noreferrer` 隔离来源页面。
- 本地服务器统一发送最小权限 CSP、摄像头/麦克风/定位禁用、同源资源、禁止嵌入与 `nosniff` 响应头。

### 验证

- 新增长 URL、`%00`、编码/原始双向控制符和安全响应头回归断言。

## 0.27.0 · 2026-08-28

### 修复

- 已有完整缓存时，导航和运行时资源遇到暂时 404/5xx 会回退对应规范缓存，不再因为服务器短暂不完整或故障拆散可运行版本。
- 在线 200 响应的运行时缓存刷新改为尽力执行；`cache.put` 因配额或存储异常失败时仍直接交付成功网络响应。
- 未知且从未缓存的运行时路径继续保留原始 404，不用应用外壳伪装成功。

### 验证

- 服务工作线程沙箱新增 HTTP 错误回退、未知资源旁路和缓存配额失败三类断言。

## 0.26.0 · 2026-08-28

### 修复

- 离线外壳预缓存改用 Cache API 原子 `addAll` 批处理；不完整部署、HTTP 失败或缓存写入失败不会留下可被误用的半套新版本。
- 保持不调用 `skipWaiting`，开放页面继续使用当前完整版本，直到浏览器正常切换到已完整安装的新工作线程。

### 验证

- 新增无依赖服务工作线程沙箱，覆盖完整安装、单资源 404 的原子失败、旧缓存定向清理、客户端接管、断网导航、查询参数规范化资源回退，以及 POST/跨源旁路。

## 0.25.0 · 2026-08-28

### 修复

- 应用销毁现在通过单个中止信号移除根节点与窗口事件，并取消状态订阅、所有短时提示定时器和周期计时器。
- 存储对象新增幂等销毁，解除跨标签 `storage` 监听并清空订阅者；旧实例不再在热重载、嵌入重挂载或测试后继续采用外部变化。
- 应用销毁会级联释放其存储对象，完整结束这一实例拥有的长期资源。

### 验证

- 使用真实 `EventTarget` 派发销毁后的存储事件，确认状态与订阅计数均保持不变；UI 契约覆盖事件中止、退订和提示定时器清理。

## 0.24.0 · 2026-08-28

### 质量

- 新增发布契约测试，递归解析从 `src/main.js` 可达的静态与动态本地模块，确保每个依赖都能经本地服务器提供并进入离线外壳。
- 离线外壳与 HTTP 白名单按集合核对，遗留孤立缓存文件或新增模块漏登记都会立即失败；所有登记文件同时验证实际存在。
- `package.json`、备份导出的应用版本与版本记录最新条目必须一致，减少手工发版漂移。

## 0.23.0 · 2026-08-28

### 修复

- 点击、选择与键盘命令现在共用同步用户操作错误边界；恢复项目、航标/解决状态、主题和示例载入遇到多标签冲突或保存失败时不再产生无反馈事件异常。
- 命令面板延迟到下一帧执行的动作也进入同一边界，未知异常显示稳定重试提示，已有精确业务错误继续原样呈现。

### 验证

- 静态 UI 契约锁定三类事件入口和延迟命令边界，防止后续新动作绕过可见错误反馈。

## 0.22.0 · 2026-08-28

### 修复

- 备份导入入口在读取前验证文件对象与可信整数大小，超过 25 MB 立即拒绝，不再让异常元数据绕过页面稳定性边界。
- 文件读取失败与畸形 JSON 统一转为稳定中文反馈，不再把浏览器或运行时的底层异常直接暴露给用户。

### 性能

- 数据保险箱按不可变工作区状态缓存备份大小；提示、焦点或同状态重投影不再重复克隆、计算校验码并序列化整份工作区。
- 新增文件大小精确边界、读取短路、元数据拒绝和解析失败测试；核心文件继续零运行时依赖并进入离线外壳。

## 0.21.0 · 2026-08-28

### 修复

- 项目归档不再依赖浏览器原生确认框，避免名称不可关联、焦点与样式不可控，以及自动化或嵌入式浏览器被原生模态框整体阻塞。
- 用户确认归档时重新检查项目和活动会话状态；对话框打开期间发生外部变化也不会把失效现场静默归档。

### 无障碍

- 应用内确认对话框通过标题与说明建立可访问名称，明确显示目标项目和全部数据仍会保留，默认焦点停在取消按钮。
- 真实浏览器验证取消路径不改变正式项目；隔离源验证确认、只读归档计数与恢复路径完整可逆。

## 0.20.0 · 2026-08-28

### 修复

- 操作成功后紧接状态重投影、hash 导航或外部标签同步时，短时提示不再随整棵界面重建而提前消失。

### 改进

- 提示改由带唯一 ID 与到期时间的内存队列驱动；每次渲染只恢复仍有效的项目，原定时器统一清除队列和当前 DOM。
- 独立本地源实测建立项目后的双重重渲染：新项目页仍显示成功提示，约 3.6 秒后准时消失；测试标签和服务随后清理。

## 0.19.0 · 2026-08-28

### 修复

- 浏览器配额临界且现有滚动快照阻塞主数据写入时，程序可释放这份可再生快照并仅重试主写入一次。
- 降级保存成功后显示明确告警并建议立即导出；不再因保留短期撤销历史而牺牲最新主数据。
- 若重试仍失败，程序尽力恢复滚动快照，同时保持主数据、内存状态与订阅事件不变。

### 安全边界

- 只有 `QuotaExceededError` 或浏览器等价错误码触发降级；网络式错误、任意异常和回滚副本失败都不会清理现有快照。

## 0.18.0 · 2026-08-28

### 新增

- 新导出的 JSON 备份信封包含基于确定性 UTF-8 数据表示的 FNV-1a 32 位内容校验码。
- 导入预览显示“内容校验码已核对（非加密）”或“旧格式未含校验码”，明确完整性能力边界。

### 修复

- 校验码格式错误或备份数据被意外修改时，导入在任何工作区替换之前失败，并给出明确损坏提示。

### 兼容

- 旧版无校验码信封与原始工作区状态继续接受严格结构校验和差异预览，不被新格式锁出。

## 0.17.0 · 2026-08-28

### 改进

- 全局搜索新增按工作区状态身份缓存的只读索引，项目标题、证据文本与摘要只做一次 NFKC/大小写规范化。
- 空查询直接渲染快捷命令而不建立搜索索引；状态未变化时连续输入只扫描预规范化字符串，状态替换后按需重建。
- 查询路径不再为每条不匹配证据创建零分结果对象，只物化真实命中并继续遵守稳定排序与 100 条硬上限。
- 50,000 个候选索引专项在百毫秒量级完成；浏览器回归验证两次连续查询、五条真实结果、清空后七个命令与搜索框焦点。

## 0.16.0 · 2026-08-28

### 改进

- 新增批量复航卡投影：会话、轨迹、检查点和最后活动时间先按项目建立一次索引，再生成全部项目卡。
- 项目推荐、七日航迹平均完整度与关注清单改用批量路径，复杂度从“每个项目重复扫描所有证据”降为一次分组加各项目局部投影。
- 单项目复航卡 API 与排序、来源时间、完整度和缺口语义保持不变，真实项目页复航卡与时间线完成浏览器烟雾回归。
- 1,000 个项目、5,000 条轨迹的专项用例在数十毫秒量级完成，并验证共享轨迹数组没有被逐项目调用全量过滤。

## 0.15.0 · 2026-08-28

### 新增

- 项目舰桥与归档舱采用通用集合窗口，首屏最多渲染 12 张项目卡，每次按 12 张继续展开。
- 展开控件提供已显示/总数/剩余数量，并把键盘焦点送到第一张新出现的卡片。

### 改进

- 完整推荐排序、关注清单、概览统计与持久化数据仍覆盖全部项目；窗口只限制 DOM 投影，不改变业务语义。
- 通用集合窗口纯函数覆盖无效输入、固定批次、最后一页封顶、源顺序不变和 50,000 项首屏边界。
- 独立本地源实测 13 个项目的 12 + 1 展开路径；390px 下控制区单列、按钮等宽且无横向溢出，测试源随后关闭清理。

## 0.14.0 · 2026-08-28

### 修复

- 另一个标签页保存时，当前页面不再因完整投影刷新而清空普通对话框里尚未提交的草稿。

### 改进

- 存储订阅事件明确区分 `local` 与 `external` 来源，本地提交继续走原有关闭与重置语义，仅外部刷新启用草稿保护。
- 外部刷新以内存快照恢复对话框字段、选中状态、光标范围与焦点；文件输入、隐藏标识和已失效的下拉选项安全跳过。
- 双标签浏览器回归连续触发两次外部主题保存，验证新项目草稿、打开状态和说明字段焦点均保持，随后恢复原测试环境。

## 0.13.0 · 2026-08-28

### 修复

- 损坏的跨标签页写入被严格拒绝时，现在会立即触发界面刷新并展示可见告警，而不再只记录在存储对象中。
- 同一份无效外部原文会被记忆并去重，重复存储事件或保存重试不会持续堆积相同通知。

### 改进

- 存储通知新增一次性领取语义，启动恢复告警和运行期跨标签告警通过同一条安全渲染路径进入页面。
- 专项测试验证坏载荷不被采用、首次告警触发订阅、重复刷新不重复发射，以及通知领取后队列为空。

## 0.12.0 · 2026-08-28

### 新增

- 全局搜索空查询现在是轻量命令面板，可直接快捷记录、新建项目、撤销、导出或进入三个主视图。
- `?` 与原有 `Ctrl/⌘ K`、`/` 一样可从非输入场景唤起搜索与命令。

### 改进

- 输入关键词时仍使用原有全工作区证据搜索，清空后立即恢复命令，不混淆动作与搜索结果。
- 命令依据当前项目与滚动快照状态禁用不可执行动作，并复用既有保存、导出和导航路径。
- 浏览器验收覆盖 390px 单列回流、无横向溢出、可访问名称及搜索到快捷记录的焦点交接。

## 0.11.0 · 2026-08-28

### 新增

- 顶栏“记录”入口和 `Ctrl/⌘ Shift C` 快捷键可从任何页面捕获项目证据。
- 快捷捕获支持选择项目、六种证据类型与可选置顶；目标项目有活动会话时自动建立正确关联。

### 改进

- “下一步”型快捷记录同步更新项目第一动作，其余记录不越权改写项目字段。
- 快捷捕获只接受未归档项目，空工作区禁用入口，核心准备函数不修改原状态并有独立测试。
- 修复所有对话框的初始焦点：显式 `autofocus` 字段优先于 DOM 中更早出现的关闭按钮。
- 390px 对话框宽 339px，无横向溢出、无未命名控件；新增核心模块同步进入服务器白名单与离线外壳。

## 0.10.0 · 2026-08-28

### 新增

- 所有未归档项目轨迹都可设为/取消“置顶航标”，按钮提供准确的按下状态与焦点恢复。
- 复航卡单独展示最新三条置顶证据，保留记录类型与证据时间。

### 改进

- 置顶不改变时间线顺序，也不会越过时间证据去改写摘要或下一动作优先级。
- 归档项目继续只读展示航标状态；导入会拒绝把非布尔值静默转换成置顶。
- 390px 下双操作按钮保持回流，无横向溢出且所有可见控件都有名称。

## 0.9.0 · 2026-08-28

### 改进

- 备份严格校验覆盖时间戳、修订号、界面设置、项目颜色、文本类型与界面一致的字符上限。
- 会话、轨迹和检查点不仅要引用存在的记录，还必须属于同一项目；跨项目拼接的合法 ID 不再蒙混通过。
- 导入总量限制为 50,000 条记录，详细错误最多展开 50 项，避免恶意或意外备份拖垮预览。
- 主数据启动与跨标签同步也先做严格校验；引用损坏时不再通过归一化静默丢弃孤立记录。
- 从滚动快照恢复后首次保存会跳过损坏主数据，不让损坏原文覆盖仍然有效的恢复点。

### 验证

- 新增畸形字段、时间倒置、跨项目链接、异常记录量、错误汇报上限、损坏主数据恢复与外部标签拒绝测试。

## 0.8.0 · 2026-08-28

### 新增

- 项目与归档时间线默认只渲染最近 30 条轨迹，并按 30 条批次继续展开。
- 展开后焦点进入第一条新显示的记录，实时区域同步播报已显示与剩余数量。

### 改进

- 全量轨迹仍参与搜索、统计、复航卡和备份；窗口化只减少 DOM 数量，不改变数据语义。
- 无效时间戳稳定落在时间线末尾，同时间记录保持原顺序，排序不修改存储数组。
- 新增 10,000 条历史的输出上限测试，首屏投影固定为 30 项。
- 时间线模块纳入本地服务器白名单、离线外壳和语法检查。

## 0.7.0 · 2026-08-28

### 新增

- 导入前差异预览：分别展示项目、会话、轨迹和检查点的当前/导入后数量，以及新增、移除、同 ID 更新。
- 项目级影响清单最多展开六项并保留完整总数；大型备份不会把对话框撑成无限列表。
- 当前与备份中的活动会话都有明确警示，避免恢复时不知情地切换计时现场。

### 改进

- 文件在打开预览前完成严格结构、引用和枚举校验；取消预览不触发任何存储写入。
- 预览期间若当前工作区被另一标签更新，差异会按最新状态重算，并要求用户重新核对。
- 内容完全一致的备份会禁用重复替换；记录顺序、界面设置和当前选择的变化仍会如实披露。
- 新核心模块同时纳入本地服务器公开白名单、离线外壳和语法检查，避免线上/离线入口版本不完整。

## 0.6.0 · 2026-08-28

### 新增

- 滚动安全快照支持撤销与再次切换（重做），不创建无限历史或额外日志。
- 顶栏撤销按钮、`Ctrl/⌘ Z` 快捷键和数据保险箱中的快照说明。

### 改进

- 恢复前完整校验快照结构、引用和数据版本；损坏快照不会替换当前状态。
- 撤销后修订号继续单调递增，并沿用多标签冲突检测，旧标签无法借恢复覆盖新写入。
- 撤销本身会把当前版本放入滚动快照，因此再次操作可以无损切回。
- 备份文件输入补充准确的可访问名称；390px 顶栏加入撤销入口后仍无横向溢出。

## 0.5.0 · 2026-08-28

### 新增

- 七日航迹：本地汇总可计会话时长、会话窗口、轨迹、近距项目切换和平均复航完整度。
- 工作区关注清单：按活动会话、受阻、未决、检查点可信度、下一步证据和离开时长解释排序。
- 最常投入项目、应急停靠/中断次数与问题闭合数的简短周内回顾。

### 改进

- 统计明确标注证据边界；单次异常长会话最多计 12 小时，避免遗留计时扭曲回顾。
- “近距切换”只计算 4 小时内相邻且不同项目的会话，不把任意历史变化包装成切换成本。
- 关注清单只陈述现场缺口，不评价产出或制造惩罚性分数。
- 七日航迹在 390、800、1280px 三档视口完成无溢出浏览器验收。

## 0.4.0 · 2026-08-28

### 新增

- 全局搜索项目、工作轨迹和检查点，支持多个关键词全部匹配与稳定相关性排序。
- `Ctrl/⌘ K` 和 `/` 键盘入口；搜索结果保留来源项目、记录类型和证据时间。
- 从项目、轨迹与检查点中提取去重后的 HTTP(S) 复航资源，不发起后台网络请求。

### 改进

- 链接识别会忽略用户凭据、危险协议、畸形 URL、片段与句末标点。
- 外部资源使用新标签页及 `noopener noreferrer`，避免给来源页面窗口控制能力。
- 搜索对全角/半角和大小写进行归一化，并限制最多 100 条结果，避免输入增长造成界面洪泛。
- 390px 和 800px 视口下的搜索框、资源列表与导航名称完成浏览器验收。

## 0.3.0 · 2026-08-28

### 新增

- 证据化复航卡：检查点、检查点后变化、最近决定、未解决事项和第一物理动作分区呈现。
- 每条推断显示来源与时间，较新的下一步记录可安全覆盖旧检查点，较旧记录不会反向覆盖。
- 问题与阻塞支持“已解决 / 重新打开”，解决时间也计入项目活动。
- 推荐排序提供可读原因，并保证仅因陈旧度不会让暂泊项目压过推进中项目。

### 改进

- 快速停靠继续限制在 50% 可信度；检查点后的未收拢或中断会话会进一步下调完整度。
- 复航卡将变化限制为 3 条、决定限制为 2 条、未解决事项限制为 3 条，避免信息洪泛。
- 项目说明与第一动作分别记录证据更新时间，状态切换不会伪装成内容更新。
- 轨迹中的解决状态有清楚的视觉、按钮名称与焦点恢复行为。

## 0.2.0 · 2026-08-28

### 新增

- 快速停靠：只使用当前会话已有证据生成低置信度检查点。
- 未收拢会话识别：跨自然日、超过 12 小时或开始时间异常时给出处理入口。
- 中断并接续：原会话与轨迹完整保留，同时建立有来源关系的新会话。
- 多标签页存储同步、旧快照覆盖保护与单活动会话约束。
- 本地服务器公开文件白名单、畸形 URL 防护和 HTTP 行为测试。

### 改进

- 损坏备份在归一化前执行严格校验，不再静默丢弃孤立记录。
- 容量临界时回滚副本失败不再阻止当前数据保存。
- 中等宽度侧栏与颜色选择器补全可访问名称。
- 畸形项目链接显示安全的未找到页面，不再导致启动失败。
- 归档项目深链接改为只读现场，不再展示矛盾的活动控件。
- PWA 在线时主动刷新应用外壳，离线时回退到完整缓存。
- 应用外壳改用相对 URL，可部署在站点子目录而不假定根路径。

## 0.1.0 · 2026-08-28

- 建立零运行时依赖的本地优先 PWA。
- 完成项目、会话、面包屑、手工检查点、复航卡、归档、主题与备份闭环。
- 建立 Node 原生测试套件与响应式视觉系统。
